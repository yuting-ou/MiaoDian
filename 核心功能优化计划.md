# 妙电 · 核心功能深度优化计划（供用户审查）

> 版本：v1.2（2026-09-10）　|　状态：**D1 已裁决（不做充电控制）；待 D3/D4，未动工**
> 方法：第一性原理拆解 → 代码现状盘点 → 分期里程碑 → 两轮自审 + 对抗式审查修订（记录在 §五）
> 配套阅读：《目标模式作业书》v3、《需求文档》v1.2、《架构文档》

---

## 一、第一性原理：电池工具到底在优化什么

锂离子电池老化由四个应力因子决定（电化学共识，不是产品话术）：

1. **高电量驻留时间**——SoC 80–100% 长期驻留是插电用户最大的老化源；
2. **高温**——超过约 35°C 加速电解液与正极老化；
3. **大充放电倍率**——尤其充电末期的大电流；
4. **循环深度**——深充深放的等效循环消耗。

由此推出三条规划公理：

- **公理一（价值排序）**：只有「控制」能直接改变物理过程；「数据」和「提醒」只能改变人的行为。三大痛点的本质：延长寿命＝降低应力因子；精准续航＝放电率估计质量（剩余电量 ÷ 净放电率，别无他法）；充电可控＝应力控制权（门槛最高的能力）。
- **公理二（诚实边界）**：macOS **没有公开的充电控制 API**。Intel 的 BCLM 键是公开已知；Apple Silicon 依赖社区逆向的未公开 SMC 键位；涓流功率控制基本不可行（PD 功率协商由适配器与系统决定，且 Apple 系统末期自动降流本来就在发生——这在我们自己的 InstantAmperage 数据里可见）。承诺做不到的事＝摧毁信任，比不做更糟。
- **公理三（妙电立身）**：作业书第一性定义「妙电卖的不是功能，是信任」。每个数字可溯源、每个提示可执行、每次沉默有理由——本计划每项都过「你凭什么这么说」透镜。

**规则冲突预警（本计划最大裁决点）**：充电阈值控制与两份现行章程直接冲突——
- 作业书 §〇.4 硬约束：「**只提示不干预**：不做充电限制等替用户做主的危险操作」；
- 需求文档 §五 明确不做：「不用私有 API、不新增联网、不申请新权限」。

用户需求（充电管理第一优先级）可以推翻旧章程，但必须**落字修订**（文档工作流纪律），不能口头默许。见 §三 裁决项 D1/D2。

---

## 二、现状盘点（规划依据，全部来自代码事实，2026-09-10 复核 @ v1.25.0）

| 能力域 | 已有（文件级依据） | 缺口 |
|---|---|---|
| 电池数据 | IOPS + AppleSmartBattery + SMC 只读三层（IOKitBatteryReader / SMCPowerReader）；健康度、循环、温度、电流、电压、PD 档位全采 | 充电控制只差「写路径」（现无任何写入代码） |
| 高电量驻留 | DailyUsage 已采 soc80to90 / soc90to100 秒数（90 天）；今日驻留已在日摘要行露出（`DailySummarySection.swift:41`） | 缺本周聚合与效果对比、无模式联动 |
| 健康样本 | 每日 1 点、月折叠永久保留、换电池序列号边界（v1.3.0） | 无异常骤降判定、无置信区间表达 |
| 提醒 | 12 类通知、阈值全部可配（保养线 70–90% 已可调、默认关）、迟滞+免打扰成体系 | 缺「健康异常」第 13 类、缺场景模式预设、缺系统充电限制探测 |
| 睡眠 | SleepDrainRecord（含 pmset 断言元凶名）+ willSleep/didWake 结算 | 无「睡眠耗电分析」聚合卡 |
| 进程能耗 | SignificantEnergyReader 差分采样（面板打开时，EMA+滞回） | 无排行卡、无手动结束入口 |
| 续航 | DrainRateEstimator 库仑计数优先 + RuntimeScenarioEstimator 场景系数 | 因子精调、置信表达 |
| 报告 | 周报/月报（文本）+ CSV 5 表 | 无日/周/月能耗聚合视图 |
| 充电控制 | **零**（SMC 只读；BCLM/CHWA 系键零命中；「优化充电打架」检测只是提示） | 见公理二与 §三 D1 |
| 硬件守卫 | 无机型检测，桌面机靠卡片资格门静默降级 | 控制层必须新增 MacBook 守卫 |
| 质量底座 | 测试 741 项（构建前置门，v1.25.0 实测）、Swift 6 审计、CI | — |

---

## 三、裁决项（动工前必须拍板）

| 编号 | 问题 | 选项 | 建议 |
|---|---|---|---|
| **D1** | 充电控制层做不做 | 原 A 提示层封顶 / B 尖兵后定 / C 直接立项 | **已裁决（2026-09-09）：不做**——「立刻充电」、SMC 尖兵、控制层全部取消 |
| **D2** | 章程修订 | 原：尖兵通过后把「只提示不干预」改为「用户显式开启+fail-safe 兜底」 | **随 D1 取消**——章程一字不动 |
| **D3** | 版本线 | 视觉 v2.0.0 等你目检四态+三档后发布（删 MIAODIAN_DEBUG_MATERIAL / --miao-visual 等视觉调试注入口；**豁免 `MIAODIAN_DEBUG_OPEN_PANEL`**——ADR-5 载明它是截图流验收的唯一程序化开面板通道，iBar 隐藏图标下面板无法无人值守打开，M1–M3 全部 UI 项验证依赖它）；本计划从 **2.1.0** 起编 | 先发 2.0.0 再开工，避免版本语义混线 |
| **D4** | 节奏 | 全程 ≈23 轮 ≈ 4 夜目标模式（6 轮/夜；M0 尖兵/M4 取消后），或按周散点推进 | 维持目标模式连续夜跑 |

---

## 四、里程碑计划表

> 每项标注：第一性依据 / 现状缺口 / 方案要点 / 风险与对策 / 验证法 / 轮预算。
> 通用纪律（每项适用，不重复写）：新判定抽 nonisolated 纯函数+新断言+变异检验；UI 只做加法（CardID 加 case，不改布局骨架）；旧档 decodeIfPresent 兼容；新提醒过机制冲突审计；视觉项走截图流或标「待晨间确认」；效果类指标按作业书写明「两周后如何验证」。

### M0 探测（2026-09-09 收官）
| 编号 | 内容 | 结果 |
|---|---|---|
| S0 | SMC 写入可行性尖兵 | **取消**（D1 裁决：不做控制层，不写任何键） |
| S1 | 系统充电限制只读探测 | **已完成（本机实测）**：按住 80% 的不是「充电上限」滑杆（实际在 100%），是「优化电池充电」自适应暂停；其签名 `NotChargingReason=16777216` 静止时稳定可读（2026-09-10 复核三采样稳定；充电中负例未采样，C2 动工前补）。原生「立即充满」入口在原生电池菜单，但原生图标已被用户隐藏（嫌双图标）→ 用户需要充满时走系统设置手动关，妙电侧由 C2 一键跳转兜底 |

### M1 · v2.1.0 充电管理·提示层（你最刚需，先行；≈6.5 轮）
| 编号 | 内容 | 第一性依据 | 现状缺口 | 方案要点 | 风险与对策 | 验证法 | 轮 |
|---|---|---|---|---|---|---|---|
| C1 | 充电场景模式预设（办公 80 / 外出满充 / 长期存放 / 快充） | 应力因子 1 | 保养线可配但无一键切换 | **模式＝配置预设**（保养线+提醒开关+免打扰组合）；D1 裁决后控制层永久取消，不再保留参数裕度 | 预设切换静默失败→每次切换后面板状态回显 | 纯函数（预设→配置映射）当夜验证 | 2 |
| C2 | 系统共存逻辑（「共存/接管」之共存） | 公理三 | 已有边沿启发式打架检测+面板洞察提示（贴线±3% 边沿计数，UsagePatternAnalyzer），但不读系统真实状态、不静默我方保养提醒 | S1 正例判定输入已备（`NotChargingReason=16777216`＝「优化充电暂缓」，三采样稳定）；**负例未采（充电中该位是否为 0），动工第一步先补采一次充电中样本**。探测到→静默我方保养提醒+面板显示「系统正在暂缓充电」+一键跳系统设置电池页——**复用既有「电池设置」入口**（`openBatterySettings()`，现走 `com.apple.Battery`，BatteryPopoverView:1124），与本机验证过的 `-Settings.extension` 比对后择一收口；未探测到→按我方保养线提醒 | 与免打扰/满充提醒/既有打架洞察叠加→过机制冲突审计 | 纯函数判定+合成输入测试+充电中实机采样一次 | 1.5 |
| C3 | 驻留效果追踪卡 | 应力因子 1 的度量 | 数据已有；今日驻留已在日摘要行显示（`DailySummarySection.swift:41`） | **扩展既有日摘要驻留行，不新建卡片**：补本周 80–100% 聚合+「提醒响应前后对比」 | 数据不足时空态诚实（作业书第一次透镜）；重复卡片风险→只做加法扩展既有行 | 当夜 UI 截图流；效果两周后验证 | 1.5 |
| C4 | 涓流可视化（原「涓流控制」降级） | 公理二：控制不可行，如实呈现 | InstantAmperage 已采 | 充电末期电流衰减曲线露出+「系统涓流中」文案；不承诺任何控制 | 文案被误读为可控→过用户认知透镜 | formatter 断言当夜验证 | 1 |
| C5 | 存放模式引导卡 | 应力因子 1 | 无 | 50–60% 存放建议+当前电量判定（纯提示） | 过早下结论→数据门槛门控 | 纯函数判定 | 0.5 |

### M2 · v2.2.0 健康管理增强（≈6 轮）
| 编号 | 内容 | 第一性依据 | 现状缺口 | 方案要点 | 风险与对策 | 验证法 | 轮 |
|---|---|---|---|---|---|---|---|
| H1 | 健康度口径与可信度表达（原「精准校准」降级） | 公理三 | 已直读 AppleRawMaxCapacity（比系统设置更实时、带分子分母）——**此项大半已达成** | 补：与系统口径差异的一句解释、估计值标注（先例 v1.6.5） | 用户拿系统数字对不上→文案过认知透镜 | formatter 断言 | 1 |
| H2 | 老化趋势增强 | 因子 1+4 | 线性外推已有（「距 80% 约 X 天」），月折叠+换电池边界已有 | 双锚点（容量斜率×循环当量）+置信区间标注；样本不足时明说「估计」 | 假精度=信任事故→置信区间强制展示 | 纯函数+变异检验 | 2 |
| H3 | 第 13 类提醒·健康异常 | 公理三「报警」 | 无骤降/持续高温/循环里程碑判定 | 健康度 30 天降幅超阈、温度持续超阈（HourlyTempStats 已有）、循环数里程碑；阈值进 AppConfiguration | **前置审计：与体检评分/12 类提醒/免打扰不打架**（机制冲突透镜） | 纯函数+合成输入测试 | 2 |
| H4 | Intel 校准引导（末位） | 因子 3 的校准特例 | 无 | 一次性引导卡（满充→放空→满充）+校准期数据标记（跳变事件通道已有）；注明不建议频繁做 | macOS 26 是 Intel 最后一代，价值递减→只做内容卡不做过深联动 | 纯函数判定 | 1 |

### M3 · v2.3.0 能耗监控增强（≈8.5 轮）
| 编号 | 内容 | 第一性依据 | 现状缺口 | 方案要点 | 风险与对策 | 验证法 | 轮 |
|---|---|---|---|---|---|---|---|
| E1 | 睡眠耗电分析卡 | 痛点「合盖掉电快」 | 元凶数据已有，零聚合展示 | 睡眠期掉电 %/小时+断言元凶排行+建议（合盖场景按 clamshell 判定补齐） | 建议被误读为指令→只建议不动作 | 纯函数聚合+截图流 | 2 |
| E2 | 进程能耗排行卡 | 痛点「定位耗电大户」 | 差分采样已有（面板开时） | Top 5 排行卡+手动结束入口：**二次确认+系统关键进程过滤+绝不自动杀** | 误杀→确认对话框+responsibility 归因已走父进程 | 采样纯函数已有测试；结束入口当夜人工验证 | 2.5 |
| E3 | 日/周/月能耗报告升级 | 公理三「复利数据」 | DailyUsage 90 天+24 桶小时统计已有 | 聚合纯函数+报告构建器/CSV 扩展；**诚实边界：不承诺月级电量曲线（细粒度只有 24h）** | 跨月数据口径漂移→dayKey 时钟防护先例复用 | 纯函数+合成跨月数据 | 2.5 |
| E4 | 动态续航精调 | 公理一「估计质量」 | 库仑计数+场景系数已有 | 窗口自适应（负载突变响应）+场景系数本机自校准（历史 drainedPercent 回归）；亮度/网络因子**先过能量悖论审计再决定加不加**（加也只事件驱动不轮询） | 过拟合单机样本→系数有界夹紧 | 纯函数+两周精度跟踪（±10% 目标） | 1.5 |

### M4 · v2.4.0 充电控制层 —— **已取消（用户裁决 2026-09-09）**

「立刻充电」、SMC 写入、充放上下限引擎不再做；章程「只提示不干预」维持原样。出行需要充满时：系统设置 → 电池 → 关「优化电池充电」（或调「充电上限」滑杆），妙电 M1-C2 提供诚实状态显示与一键跳转。原 K1–K5 设计随裁决删除。

### M5 · v2.4.0 收尾（≈2 轮）
| 编号 | 内容 |
|---|---|
| W1 | 两周后验证复盘：C3 驻留、E4 精度；经验账本沉淀 |
| W2 | 文档对齐：需求文档 §七 演进记录、架构文档、AGENTS.md、CHANGELOG |

**总预算**：≈23 轮 ≈ 4 夜（6 轮/夜；M0 尖兵/M4 取消后。对账：M1 6.5＋M2 6＋M3 8.5＋M5 2＝23）+ 你的目检配合点：M3 E2 结束入口、各里程碑面板视觉确认。

---

## 五、自审记录（两轮，供你检查我是怎么审的）

### 第一轮（初稿 → 修订）
1. **初稿默认做了充电控制**——与作业书 §〇.4 硬约束直接冲突却未处理 → 拆成 D1 裁决项+尖兵门控，控制层独立成 M4。
2. **「涓流充电控制」照单立项**——实际 PD 功率协商应用无法干预，且 Apple 末期自动降流本就存在 → 降级为 C4 可视化，公理二写明。
3. **「精准健康度校准」立项做读取**——代码已直读 AppleRawMaxCapacity（比系统显示更实时），是重复造轮子 → 降级为 H1 可信度表达。
4. **「放电下限」与「上限」当成两个功能**——同一 SMC 禁止机制的两侧 → 合并进 K2 一个引擎。
5. **Intel 校准引导排位过高**——你在 Apple Silicon + macOS 26，Intel 仅剩最后一代支持 → 降到 H4 末位。
6. **「历史能耗统计」差点承诺月级电量曲线**——细粒度 SOC 曲线只有 24 小时 → E3 写明诚实边界。
7. **「一键结束进程」没写防护**——补二次确认+系统进程过滤+绝不自动杀（E2）。

### 第二轮（修订稿 → 定稿）
1. **尖兵没有退出条件**——补「超时 1 夜或失败→永久降级提示层，不再复议」，防止整条充电线悬置。
2. **尖兵的机器安全协议没落字**——补 S0 协议：你全程在场、写入读回即恢复、拔适配器物理兜底、不进产品代码、不自动化。
3. **里程碑顺序没回应「充电最刚需」**——调为 M1 提示层先行（即使将来上控制层，共存与模式预设也是它的地基），控制层独立 M4 由裁决+尖兵把门。
4. **模式预设没定义抽象**——补「模式＝配置预设」，控制层有无不改变该抽象，避免 C1 返工。
5. **新增提醒没过机制冲突透镜**——H3 加前置审计（与体检评分/12 类提醒/免打扰叠加）。
6. **没算总账**——补轮预算对账：≈28–32 轮≈5 夜，与产能匹配；你的配合点单列。
7. **视觉项无人值守验收**——面板打不开是已知约束（iBar 隐藏图标）→ 通用纪律注明截图流/待晨间确认，先例 v1.24.0。

### 第三轮（对抗式审查修订，2026-09-10——逐条代码/真机实证后落字）

1. **C3「零展示」不实**——今日 80%+ 驻留已在日摘要行显示（`DailySummarySection.swift:41`，经 `BatteryPopoverView.swift:791` 渲染）→ 现状缺口如实改写，C3 改为**扩展既有行不新建卡**，防重复卡片。
2. **C2 深链与产代码脱节**——面板菜单已有「电池设置」入口（`openBatterySettings()` 走 `x-apple.systempreferences:com.apple.Battery`，`BatteryPopoverView.swift:1124`），原稿只写 `-Settings.extension` 未提既有入口 → 改为复用既有入口，两标识符真机比对后择一收口。
3. **判定输入只有正例**——`NotChargingReason=16777216` 三采样稳定（2026-09-10 复核），但当下电池正被系统按住，充电中负例（该位是否为 0）零样本 → C2 动工第一步补采一次充电中样本，验证法加实机采样。
4. **全文数字对账**——测试 728→741（v1.25.0 已于 2026-09-09 凌晨发版）；盘点快照 @ v1.24.2→v1.25.0；D4 行残留取消前 28–32 轮→按 §四 对齐；M1/M3 表头轮数小于各行合计（6.5/8.5）→ 表头改准，总预算 23 轮精确化。
5. **D3 删注入口与验收通道冲突**——ADR-5 载明 `MIAODIAN_DEBUG_OPEN_PANEL` 是截图流验收唯一程序化开面板通道（iBar 隐藏图标、面板不可无人值守打开是硬约束，账本 2026-09-07 再证）→ D3 豁免它，只删 MIAODIAN_DEBUG_MATERIAL / --miao-visual 等视觉调试注入口。
6. **C1 残留取消前裕度**——「控制层若立项附带 SMC 参数」随 D1 失效 → 删除，明示控制层永久取消。

其余代码事实声明经实证存活：三层只读无写路径、BCLM/CHWA 零命中、CSV 5 表、AppleRawMaxCapacity 直读、线性外推、驻留秒数 90 天、EMA+滞回、responsibility 父进程归因、无机型检测、保养线 70–90% 可调默认关、/Applications 1.25.0＝最新 tag 存活。

---

## 六、成功指标（计划做完看什么，不只看版本号）

1. **驻留改善**：80–100% 驻留时长相对下降（以 C1 模式预设+保养提醒为干预手段，前后各两周对比，数据现成）；
2. **续航误差**：预估 vs 实测掉电偏差收敛到 ±10%；
3. **信任底线**：测试 741 项只增不减、零新增静默失败、能量红线（面板关 CPU≈0.1%）不破。

## 七、给你的审查清单

1. ~~D1~~ **已裁决（2026-09-09）：不做充电控制**——尖兵/控制层取消，章程不动
2. ~~D2~~ 随 D1 取消
3. D3：先发 2.0.0（等你目检）再开工，还是先做本计划？
4. D4：连续夜跑还是按周散点？
5. M1–M3、M5 各项：砍哪个、加哪个、调顺序？
