# 妙电面板 · 液态玻璃改造计划（Liquid Glass）

> 目标：把状态面板与设置窗口迁移到 Apple 的 Liquid Glass 设计语言（macOS 26 Tahoe），在保留妙电"数据密度 + 信任"内核的前提下，获得系统级的通透质感。
> 本文件是计划，不含实现。每个阶段有明确的进入/退出条件与验证方式。

---

## 〇、第一性：Liquid Glass 到底是什么（避免做成"假玻璃"）

Liquid Glass ≠ 半透明模糊。它是一种**会折射背后内容、带镜面高光边缘、并对交互有弹性反馈的动态材质**。Apple 的官方立场很明确：

1. **玻璃是"浮在内容之上的层"**——工具栏、面板、控件、胶囊徽章；**内容本身不是玻璃**。妙电的内容是数据（曲线、数字、热力图），它们必须清晰可读，不能被玻璃糊住。
2. **一层玻璃原则**——避免"玻璃叠玻璃"的廉价感。一个面板应当是**一张玻璃板**，卡片在其上以极淡的层次/发丝线区分，而不是每张卡片各自一块玻璃。
3. ** tint 而非实色**——要颜色时用玻璃的 tint（如充电绿、低电红），而不是实心填充。
4. **交互即材质**——悬停/按压时玻璃有弹性形变与高光流动，这是"液态"的灵魂。

对应到妙电：**面板外壳 = 一张玻璃板；卡片 = 玻璃上的极淡分区；控件/徽章/仪表 = 可交互玻璃；图表 = 保持清晰的内容层。**

---

## 一、必须先拍板的三个决策（阻塞开工）

### 决策 1：部署目标（最关键）
Liquid Glass API（`glassEffect` / `GlassEffectContainer` / `Glass` / `AnyShapeStyle`）是 **macOS 26.0+**。妙电当前最低 macOS 15.0、universal 二进制。三选一：

- **A. 抬到 26.0**：全量用玻璃，代码最干净；代价是放弃 macOS 15–25 用户（含部分 Intel Mac）。
- **B. 保持 15.0，玻璃用 `if #available(macOS 26.0, *)` 双路径**：26 上玻璃、15–25 上保留现有质感作降级。工作量 +~40%，但保住开源项目的覆盖面。**← 我推荐 B**（妙电已开源，覆盖面是资产；降级路径正好复用现有 PopoverCard）。
- **C. 抬到 26 但保留降级**：仅当你确认不需要 15–25。

### 决策 2：玻璃浓度（审美定调）
- **克制派**：只有外壳 + 控件用玻璃，卡片维持现状（改动小、风险低、数据最清晰）。
- **标准派（推荐）**：外壳一张玻璃板，卡片降为极淡 tint 分区，控件/徽章/仪表全玻璃化 + 交互，入场动效改弹性。
- **激进派**：连图表容器、菜单栏图标都做玻璃（风险：可读性、性能、"玻璃汤"）。

### 决策 3：范围
仅状态面板？还是含**设置窗口**（Form 在 26 上本身会部分自动采纳玻璃，需统一调）与**充电曲线独立窗口**？建议：面板 + 设置窗口一起做（同一套材质 token），曲线窗口随后。

---

## 二、技术未知数（Phase 1 必须先探明，否则后面全白做）

1. **MenuBarExtra(.window) 能否承载自定义玻璃背景**：它的窗口 chrome 由系统托管，可能盖不住/透不出桌面。若不行，退路是改用 AppKit `NSPanel` + `NSVisualEffectView`（`.hudWindow`/`.underWindowBackground`）自持内容，或接受系统材质只做内部元素玻璃化。**这是 go/no-go。**
2. **`GlassEffectContainer` 与 Canvas 图表共存**：图表是 Canvas 自绘，玻璃容器包裹时的性能与裁剪行为需实测。
3. **`supportsGlassEffect` / 降级探测**：确认在 15–25、以及"降低透明度/增加对比度"辅助功能开启时，系统给什么回退。
4. **SDK 校验**：本机 CLT 是 26 SDK，`#available(macOS 26.0)` 编译无碍；CI 的 macos-26 runner 正好能验证玻璃路径编译。

---

## 三、材质映射表（现有 → 玻璃，作为全局 token）

| 元素 | 现状 | Liquid Glass 目标 |
|---|---|---|
| 面板外壳 | 系统弹窗材质 + padding | 一张 `Glass.regular` 板，圆角 `.continuous` 大半径，边缘镜面高光 |
| PopoverCard | `quaternarySystemFill` + 0.06 描边 | 去掉实心底，改极淡 tint 分区或发丝线；或整卡 `glassEffect(.regular.tint(...).interactive)` 但**克制派下维持淡分区** |
| 状态徽章（充电/低电/充满） | Capsule 实色 13% 透明 | **bold 玻璃胶囊 + tint**（绿/红），交互微弹 |
| 头部仪表环 | Circle stroke + AngularGradient | 玻璃圆 + tint 进度，中心图标浮于玻璃上 |
| 控制行（设置/导出/退出） | HoverHighlightRow 0.08 灰底 | **可交互玻璃行**，悬停高光流动、按压弹性 |
| 图表（功耗/温度/24h/热力图） | Canvas 自绘 | **保持内容层不玻璃化**；容器可玻璃；描边加轻微高光渐变但优先可读 |
| 菜单栏标签 | 自绘 NSImage | 不动（菜单栏本身在 26 已是玻璃，图标保持模板色自适应） |
| 入场动画 CascadeIn | opacity+offset+scale 错峰 | 玻璃弹性入场（scale+blur 收拢，spring 更软），保留错峰节奏 |

---

## 四、分阶段执行（每阶段独立可发布、可回滚、可截图验收）

### Phase 0 · 决策与基线（0.5 天）
- 你拍板决策 1/2/3。
- 建立**视觉基线**：现在的面板/设置窗口各截一张图存档（`docs/glass/before/`），作为对比锚。
- 产出：材质 token 草案 + 本计划定稿。
- 退出：三决策确定，基线截图就位。

### Phase 1 · 技术探针（1 天，go/no-go）
- 在分支上做一个最小验证：MenuBarExtra 窗口能否透出桌面 + 承载 `glassEffect`；`GlassEffectContainer` 包 Canvas 的帧率与内存。
- 量一次**性能预算**：面板打开时 CPU/GPU、关闭时是否归零（能量悖论红线）。
- 产出：探针结论 + 是否需退到 NSPanel 方案。
- 退出：外壳玻璃可行性确认，性能在预算内。**若不可行 → 降级为"仅内部元素玻璃化"，回到决策 2 重定浓度。**

### Phase 2 · 外壳与卡片材质（1–1.5 天）
- 落地材质 token：外壳玻璃板 + 卡片淡分区（按决策 2 浓度）。
- 全部 `#available(macOS 26)` 包裹，15–25 走现有 PopoverCard（决策 1=B 时）。
- 验证：截图对比 before；480 测试零回归；Swift 6 零错误；`降低透明度` 开启时回退正常。
- 退出：外壳+卡片在 26 上是玻璃、在 15–25 上不劣化。

### Phase 3 · 控件与徽章交互玻璃（1 天）
- 控制行、状态徽章、头部仪表 → 可交互玻璃 + tint。
- 悬停/按压弹性（`.interactive`）。
- 验证：截图 + 交互录屏（可选）；VoiceOver 朗读标签不丢（无障碍是妙电既有资产，不能退化）。
- 退出：控件有"液态"反馈，无障碍不回退。

### Phase 4 · 动效与形变（0.5–1 天）
- CascadeIn → 玻璃弹性入场；`GlassEffectContainer` 让相邻玻璃元素在状态切换（如充电↔放电、展开↔折叠卡片）时**形变过渡**而非硬切。
- 红线：动效只在面板打开时存在，关闭零开销。
- 退出：入场与状态切换有玻璃质感，无掉帧。

### Phase 5 · 设置窗口统一（0.5–1 天）
- Form 在 26 的材质与面板对齐；阈值 Picker、开关、数据存档按钮玻璃化。
- 退出：两处窗口同一套语言。

### Phase 6 · 无障碍 / 性能 / 降级加固（0.5 天）
- 对比度：玻璃在任意桌面壁纸下的可读性抽查（浅色/深色/花壁纸三张）。
- "区分颜色/增加对比度/降低透明度"三种辅助设置下的回退。
- 稳态 CPU 复测（对齐 v1.6.10 的 ~0.1% 基线，确认玻璃没让后台变贵——玻璃只在打开时渲染，预期无影响，但要测）。

### Phase 7 · 验收与发布（0.5 天）
- 逐阶段截图归档 `docs/glass/after/`。
- 版本号：这是设计语言迁移，按次版本走（如 v1.7.0）。
- 打包 → 安装 → 推送 → Release（附 DMG + 前后对比图）。
- 退出：全绿 + 你亲眼确认。

**总量级**：约 5–7 个工作日（决策 1=B 双路径取上限）。可分阶段发布，不必一次做完。

---

## 五、验证纪律（吸取 ScrollView 空白事故）

1. **每一步都要截图验收**——屏幕录制权限已开，这是上次翻车后补上的关键通道。
2. 面板被 iBar 隐藏、无法无人值守打开 → **每阶段需要你点一下菜单栏图标**，我截图核对；或你授权我临时把图标从 iBar 移出。
3. 逻辑层零改动：这是纯 presentation，480 测试必须始终全绿；任何测试变红 = 碰到了不该碰的东西，立即回滚。
4. 每阶段一个可回滚提交，玻璃改动集中在新增 `Glass*` 视图/修饰符 + 少量 `#available` 分支，不散改数据流。

---

## 六、明确不做（守住妙电的内核）

- 不做"玻璃汤"：图表、数字、热力图是内容，保持清晰，不套玻璃。
- 不为美观牺牲数据密度：妙电的价值是"一眼看到很多真东西"，玻璃是容器不是主角。
- 不引入第三方玻璃库（依赖红线）；不用私有 API。
- 不在 macOS 26 以下强上玻璃导致降级路径破相。

---

## 七、风险登记

| 风险 | 概率 | 缓解 |
|---|---|---|
| MenuBarExtra 窗口承载不了自定义玻璃 | 中 | Phase 1 探针先验；退路 NSPanel 或仅内部玻璃化 |
| 玻璃下数据可读性下降 | 中 | 内容不玻璃化 + 对比度三场景抽查 + 辅助功能回退 |
| 性能/能耗回退 | 低 | 玻璃仅打开时渲染；Phase 6 复测稳态 CPU |
| 双路径（15–25）维护成本 | 中 | 降级路径 = 现有 PopoverCard 原样保留，不重写 |
| 无法无人值守视觉验收 | 高 | 每阶段请你点一次图标配合截图 |

---

## 八、下一步（等你回复即可开工）

请拍板：**决策 1（部署目标 A/B/C）、决策 2（玻璃浓度）、决策 3（范围）**。
定了我就从 Phase 0 基线截图 + Phase 1 探针开始，探针结果出来先给你看，再决定外壳方案。
