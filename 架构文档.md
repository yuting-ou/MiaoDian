# 妙电 · 液态玻璃改造 架构文档

> 版本：v1.1（2026-09-05）　|　需求来源：《需求文档.md》　|　过程记录：《玻璃改造报告.md》

## 一、总体架构

```
┌─ 菜单栏层 ────────────────────────────────┐
│ NSStatusItem（8 种标签模式，MenuBarPanelController） │
├─ 面板宿主层 ──────────────────────────────┐
│ GlassPopoverPanel：自建透明 NSPanel                │
│  · level=.statusBar  · 非激活  · 失焦即关          │
│  · debugKeepOpen（MIAODIAN_DEBUG_OPEN_PANEL 调试） │
├─ 服务层 ─────────────────────────────────┐
│ AppServices 单例：Monitor / Config / History /      │
│ AlertController / IconAnimator / BehaviorCoordinator│
├─ 内容层（不可玻璃化）────────────────────┐
│ BatteryPopoverView → 17 卡体系（PopoverCard 发丝分区）│
│ Canvas 图表 / 数字 / 热力图 —— 清晰内容层            │
├─ 材质层 ─────────────────────────────────┐
│ GlassStyle.swift：panelShell / cardSection /         │
│ GlassRow / GlassBadge / gaugeGlassBase / tintedTile  │
│ （#available(macOS 26) 玻璃 / 15–25 原质感 双路径）  │
└──────────────────────────────────────────┘
```

辅助窗口：设置 = SwiftUI `Settings` 场景（AppKit 动作唤起）；充电曲线 = AppKit
`ChargeCurveWindowController`（面板由 NSHostingView 承载后场景环境动作不可用）。

## 二、文件地图（本次改造触及）

| 文件 | 职责 |
|---|---|
| `Core/MenuBarPanelController.swift` | 状态项 + 透明面板 + 标签 8 模式 + 调试通道 |
| `Core/AppServices.swift` | 全局服务单例（对象收口） |
| `Core/ChargeCurveWindowController.swift` | 曲线窗口（AppKit） |
| `UI/Components/GlassStyle.swift` | 材质 token 与可用性感知修饰符（唯一材质入口） |
| `UI/Components/PopoverComponents.swift` | PopoverCard 分区 / GlassRow / CascadeIn(减动效) |
| `UI/BatteryPopoverView.swift` | 面板骨架（frame 292/584、分列算法未动）+ panelShell |
| `UI/BatteryHeaderView.swift` | 头部 + 仪表玻璃底 + 呼吸光点(减动效) |
| `UI/*Sections*.swift` | 徽章/图标块双路径、热力图配色去重 |
| `UI/Components/ChargeCurveDetailView.swift` | 曲线窗口容器/按钮双路径 |
| `测试/预览/` | 半透明窗口预览 harness（视觉验收工具） |
| `工具/对比度测量.swift`、`工具/壁纸实测.sh` | WCAG 实测与三壁纸脚本 |
| `build.sh`、`测试/run_tests.sh` | 部署目标 15.0（单一定义处） |

## 三、材质系统设计（可读性优先的分层）

| 层 | 26 路径 | 15–25 降级 | 可读性护栏 |
|---|---|---|---|
| 外壳 panelShell | ultraThinMaterial + windowBackgroundColor 0.5 亮度地板 | 同（无需分支） | 亮度地板保证任意壁纸下黑字/白字对比度；实测记录见 ADR-3 |
| 卡片 cardSection | 极淡填充 0.045 + 发丝线 0.09（增强对比 0.10/0.22） | quaternarySystemFill + 0.06 | 分区不加模糊，内容对比由外壳统一 |
| 控件 GlassRow | clear+interactive 玻璃药丸 | 悬停灰底 | 行标签 ≥AA |
| 徽章 GlassBadge | clear + tint 0.5 | 实色 13% | 文字用语义色本身 |
| 仪表 gaugeGlassBase | clear 玻璃圆 | 无 | 中心数字清晰优先 |
| 图表/数字 | **内容层，永不玻璃化** | 同 | N1/N2 |

## 四、技术决策记录（ADR）

**ADR-1 部署目标保持 15.0 双路径**
背景：玻璃 API 为 26 独占。曾短暂升到 26.0 单路径。
决策：回 15.0，`#available(macOS 26)` 分支，降级=原质感。理由：开源覆盖面是资产（需求 F3）。
后果：样式代码成对出现，由 GlassStyle 单点收口控制维护成本。

**ADR-2 面板宿主 = 自建透明 NSPanel（替换 MenuBarExtra(.window)）**
背景：探针证实 MenuBarExtra 宿主灰底由 SwiftUI 图层自绘（视图树无 NSVisualEffectView），
AppKit 无法清除，玻璃采样不到桌面（no-go）。
决策：NSStatusItem + GlassPopoverPanel；8 种标签模式移植；曲线窗口改 AppKit 控制器。
后果：获得玻璃采样桌面的能力 + 程序化开面板的验收通道；代价是宿主代码自维护（约 250 行）。

**ADR-3 外壳材质 = ultraThinMaterial + 亮度地板（关键迭代）**
背景：外壳材质经历 clear → regular → ultraThin → ultraThin+地板 四次实测迭代。
- clear：深色壁纸上黑字 2.52:1（不达 AA）——玻璃完全跟随后壁亮度；
- regular：1.71:1，同样跟随；ultraThin：更暗（2.16 前后）。
根因：**透明面板 + 浅色模式黑字，在深色壁纸上数学上不成立**——与玻璃品种无关。
决策：材质之上叠加 windowBackgroundColor 0.5「亮度地板」（自适应：浅色外观=白、深色=黑）。
实测：深色壁纸下头部大数字 16.04:1（AAA）。代价：壁纸色彩透出变柔和——
按需求裁决规则「玻璃让一步，可读性不让步」执行，并在需求文档 §七 记录演进。

**ADR-4 卡片降发丝分区，不各自成玻璃**
背景：卡片各自 glassEffect 出现"玻璃汤"（双层材质互叠、内容糊底）。
决策：外壳一整块底 + 卡片极淡分区/发丝线；GlassEffectContainer 随之移除。
后果：内容对比度由外壳统一保证；相邻玻璃形变效果让位于可读性。

**ADR-5 调试通道 MIAODIAN_DEBUG_OPEN_PANEL**
背景：面板被 iBar 收进隐藏区，无法无人值守打开（原计划列为高风险）。
决策：环境变量触发自动弹出（0.6/2/5s 三次重试兜底状态项就位竞态）+ debugKeepOpen 豁免失焦关闭。
后果：视觉验收不再依赖用户点图标；生产路径无该环境变量时零影响。

## 五、阶段拆分与状态（复杂任务分阶段执行）

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 基线 | before 截图 + token 草案 | ✅ |
| P1 探针 | MenuBarExtra no-go → NSPanel；CPU 实测 | ✅ |
| P2 外壳与卡片 | 双路径 + 发丝分区 | ✅ |
| P3 控件徽章 | 交互玻璃 + tint + 无障碍保留 | ✅ |
| P4 动效 | 三动效点接减少动态 + 增加对比度分支 | ✅ |
| P5 设置窗口 | 26 系统自动采纳，零改动核对 | ✅（截图待补） |
| P6 可读性加固 | 对比度实测全表 ✅ / 三壁纸 ⏸ / 三开关 ⏸ | 部分 |
| P7 发布 | CHANGELOG + v1.7.0 + Release | 待 P6 收尾 |

## 六、验证体系

1. **逻辑门**：480 项自制 harness 测试全绿 + 零警告（与主程序共用源文件，测真代码）。
2. **并发审计**：Swift 6（15.0 目标、MainActor 隔离）typecheck 零错误。
3. **对比度实测**：`工具/对比度测量.swift`（CGImage 直读 RGBA，测试图仲裁过坐标映射）。
4. **视觉验收**：`测试/预览/` harness + `MIAODIAN_DEBUG_OPEN_PANEL` 程序化开面板 + 截图。
5. **能量实测**：top 采样面板开/关 CPU（关闭态 0.0%）。

## 七、风险与遗留

| 项 | 状态 |
|---|---|
| SignificantEnergyReader cString 弃用警告 | 遗留（逻辑文件，视觉改造期不动） |
| 橙/绿语义数字对比度 3.25/2.46 | 既有特性（基线对照非回归），待专项决策 |
| 三壁纸/三开关截图 | 待用户配合（动态壁纸 App 需暂停；开关同页 2 分钟） |
| 状态项就位竞态 | 已三次重试兜底；若再现按作业书流程记录 |
