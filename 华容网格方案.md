# 华容网格方案（面板 v2）

> 状态：待对齐（未实施）。目标版本 v1.13.0。
> 前置：v1.12.0 华容道布局编辑（PanelLayout 左/右/隐藏三表 + 常态拖拽把手 + 编辑模式隐藏管理）已交付。

## 一、现状与差距

当前棋盘是「两根独立竖列」：每张卡固定半宽（约 277pt）、高度自由，`balancedSplit` 按预估高度贪心配平。这其实不是华容道——华容道的精髓是**块有不同尺寸**，而我们现在只有一种宽度。具体痛点：

1. **横向浪费**：高耗电应用（应用名长）、充电记录（时间戳+时长）、今日用电（图+明细）这类横向信息在半宽里被挤压截断。
2. **图表太挤**：MiniChartSections 是 Canvas 自适应宽度，半宽下曲线分辨率吃亏；满宽曲线可读性高一截。
3. **没有"并排/独占"的表达**：配平只看高度，用户无法说"这两张我想并排"或"这张我要独占一行"。
4. **托盘放回位置丢失**：`unhide(to: .right)` 固定塞右列末尾，放回后还要再拖一次。

## 二、核心想法：把「跨列」补成真正的华容块

华容道块型：1×1、2×1（横）、1×2（竖）、2×2。我们的棋盘是 2 列宽：

- **竖块 = 半宽卡**（现状，高度自由）——已有
- **横块 = 跨列卡**（独占整行）——**本方案新增**，补齐块型

## 三、棋盘模型：带（Band）

棋盘 = 竖向的**带**序列。每条带是二者之一：

- **跨列卡独占一行**（2 格横块）；
- **一组半宽卡**，带内按现有预估高度贪心配平成两列（`balancedSplit` 逻辑原样复用，作用域从全板缩小到带内）。

跨列卡天然是带的分隔线。条件不可见的卡保留在序列中、渲染跳过（现状机制不变）；带内可见卡清零时整条带塌缩，不占空。

### 数据模型（PanelLayout v2）

```swift
nonisolated struct PanelLayout: Codable, Equatable, Sendable {
	var flow: [String]        // 已放置卡的阅读序（含跨列卡，去重不变式同 v1）
	var wide: Set<String>     // 跨列成员（必须在 flow 中）
	var hidden: [String] = [] // 托盘
}
```

**v1 → v2 迁移**：旧三表 `{left, right, hidden}` 解码时转 `flow = left + right`（各列内相对序保留，无跨列卡），旧字段不删、读时兼容，留回滚路径。转换单元测试进 harness。

**有意的取舍**：跨列卡把"列边界"打散后，v1 的"精确列位"不再可表达；列归属改为带内自动配平，位置语义收敛为**阅读序中的位置**（拖到哪 = 排哪，落点实时预览可见）。若日后确需钉死某列，再加 `pinLeft` 集合，本期不做。

## 四、渲染

```
VStack(spacing: 8) {
	ForEach(bands) { band in
		band.isWide ? 跨列卡 : HStack(alignment: .top) { 左列, 右列 }
	}
}
```

- 带内左/右列 = 现有 `cardColumn` **原样复用**：CardFrameProbe 探针、拖拽把手、编辑控制条都不动。改动集中在切带与顶层容器。
- 跨列卡宽度 = 面板全宽（584 − 24 padding ≈ 560pt），图表走 `chartCanvas(width: geo.size.width)` 自适应，无需改绘图层。

## 五、交互

| 通道 | v1 现状 | v2 变化 |
|---|---|---|
| 常态拖拽（把手） | 列内/跨列移动，落点 (列, 锚点) | 落点改为 (锚点卡, 前/后)，插入阅读序；跨列卡上下缘即插入点 |
| 编辑模式控制条 | 仅眼睛（隐藏） | 加一枚「跨列/收窄」切换键（SF: `rectangle.expand.vertical` / `rectangle.compress.vertical`） |
| 隐藏托盘放回 | 固定插右列末尾 | 追加到 flow 末尾，可再拖微调 |
| 重置布局 | 无 | 编辑工具条加「重置」，一键回到出厂带式默认（可选，第 3 步） |

## 六、默认布局（出厂 seed，v2 首次播种用）

沿用 v1.10.0 语义分簇，用带表达簇边界：

| 棋盘 | 卡片 | 理由 |
|---|---|---|
| 带 | 充电协议与功率 \| 电池状态 | 双状态并排，现状即如此 |
| **跨列** | 今日用电 | 图+横条明细，宽度收益最大 |
| 带 | 24小时电量 \| 健康度趋势 | 双曲线并排对比 |
| 带 | 功耗曲线 \| 温度曲线 | 双曲线并排 |
| 带 | 时段用电 \| 用电日历 | 时段类并排 |
| **跨列** | 充电记录 | 时间戳+时长横向更有余量 |
| 带 | 续航换算 \| 洞察 | 短卡归拢 |
| **跨列** | 高耗电应用 | 应用名不再截断 |
| 带 | 电源事件 \| 蓝牙外设 | 外设与事件簇 |
| 带 | 电池身份证 \| （体检仍走头部） | 尾部收拢 |

已存 v1 布局的用户迁移后**保持原样**（无跨列卡），上表只对全新播种生效。

## 七、配平与稳定性（不洗牌）

- 带内配平 = 现有贪心，按带重入。
- 「面板打开期间不洗牌」约束升级为**冻结棋盘**：打开时固化切带与带内分列（沿用 `assignedLeft/Right` 思路），数据陆续到齐时新半宽卡追加到所在带较矮列、新跨列卡固定追加为最末带；关面板重开才重新整体配平。
- 预估高度表沿用 `visibleCards` 现有估算，跨列卡按全宽高度重新校准常数。

## 八、测试（构建门，全绿才编译）

`PanelLayoutEditor` 重写为 flow 版纯函数，新增用例进 `测试/run_tests.sh`（现有 540 例之上叠加）：

1. 切带与带内配平（跨列卡分隔、带塌缩、空板）；
2. move/hide/unhide/normalize 全套（去重、未知 id 过滤、缺失追加、幂等）；
3. v1→v2 迁移（旧 JSON 样例解码）；
4. `CardDropResolver` 扩展的落点几何：跨列卡 frame 上/下半判定、列带外扩 20pt 边界、与两列 frame 共存时的优先级。

## 九、风险与验证（对齐经验账本）

- **拖拽几何是历史事故高发区**（ScrollView 空白面板教训）：第 1 步只动数据层，UI 零变化；第 2 步接 UI 后必须用 `MIAODIAN_DEBUG_OPEN_PANEL` 常驻调试面板**分阶段截图**（半宽/跨列混排、托盘、拖拽中让位、玻璃面板只能全屏截）才算验证。
- **预览几何基准**：拖动预览沿用"位移以起始几何为基准"的现有约定，切带重排只影响让位目标，不回灌落点计算。
- 迁移保留旧字段读取路径，出问题可整体回退 v1 渲染。

## 十、分期落地

| 步骤 | 内容 | 验收 |
|---|---|---|
| 1 数据层 | PanelLayout v2 + flow 版编辑器纯函数 + 迁移 + 测试 | 测试全绿；UI 行为不变（v2 布局先按全半宽渲染） |
| 2 棋盘渲染 | 切带渲染 + FrameTable/resolver 扩展 + 冻结棋盘 + 编辑模式跨列键 | 调试截图通过；拖拽/托盘/跨列切换全链路手测 |
| 3 打磨 | 出厂带式默认 seed + 托盘 flow 化 + 重置布局（可选） | 版本号 1.13.0；CHANGELOG/README 同步 |
