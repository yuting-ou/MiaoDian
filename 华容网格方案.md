# 华容网格方案 v3（第一性重推版）

> 状态：**已实施并验收（v1.14.0）**。终版为 v4 模型：显式行存储。
> 推导链：v2（贪心+宽卡默认）→ v3 自检发现预览≠落盘 → v3 结构配对 → **v3 高度门实测失败（行式对齐密铺比贪心双列高 33%，默认出厂必超屏）** → v4 终局：
> ① 自动模式保持 v1.13 贪心双列（973pt 已验证塞得下，高度最优、零回归）；
> ② 自定义模式用**显式行存储** `rows: [[String]]`（一行 = 宽块独占 或 两张半宽）——双射靠存储本身成立，零布局算法、零高度输入，预览=落盘=重开；
> ③ 宽块改为**用户显式选择**（编辑模式宽窄键）：高度门证明宽卡默认出厂不可行（独占行无法与其他内容共享竖向空间，最少 +20%），宽度消耗的是屏幕高度硬预算，归属用户意图而非系统优化——推翻 v2"宽窄归系统"的裁决。

## 1. 第一性推导

**Q1 面板是什么？** 菜单栏弹出的**瞬时瞥视面**：出现几秒到几分钟，回答"我现在还好吗"，然后消失。推论：
- 总高度受屏幕物理上限约束（v1.12 教训：1079pt 超屏、控制行被 Dock 遮挡——高度是硬约束不是审美）；
- 内容随时变（充电状态、样本暖机、开关增减），布局必须在任何数据状态下成立，包括全空。

**Q2 用户自定义布局到底要什么？** 面板看一眼就走，用户**设一次、几乎不再碰**（v1.13 实测方向：编辑模式瘦身为纯隐藏管理）。所以自定义要表达的意图只有三样：
1. **阅读序**——我关心什么先看；
2. **去留**——什么藏掉；
3. **塞得下**——别超高。

**Q3 杠杆归属。** 从 Q2 推：阅读序和去留是**意图**，必须交给用户；列归属、卡宽、并排配对是**优化**（服务于"塞得下"和可读性），应该系统自己扛。上一版把"宽窄开关"发给用户——方向反了，否决。v1.13 刚把五键控制条砍成一只眼睛，不能再加回键位。

**Q4 模型必须贴着现实。** 卡片高度由内容决定且可变（26pt 行 × N、Canvas 图表、折叠 32pt），所以**任何固定格子网格都是错的**——格子要撒谎（裁切或留洞）。唯一诚实的几何是**密铺**：按高度真实堆叠。列数为什么是 2：17 种卡最多同时 ~14 张，单列 292pt 宽面板会超 2000pt，物理上必须折行；三列则半宽卡只剩 ~260pt 且面板近 900pt 宽，瞥视面失格。2 是物理最优。

**结论（最小充分模型）：用户只拥有「阅读序 + 眼睛」，系统在序之上做带跨列的密铺。**

## 2. 模型

- **flow（阅读序）**：卡片的线性序列，拖到哪 = 排哪。列和行都从序推导，不再存储。
- **wide（跨列）**：按卡型静态判定，代码定义、不可配置：`今日用电`、`充电记录`、`高耗电应用` 三张横向内容卡（应用名/时间戳/充电器名在 277pt 半宽里截断是账本在案的老问题，v1.6.14 行尾加充电器名后更甚）。wide 卡独占整行，天然成为阅读节奏的分节线。图表卡保持半宽、相邻即并排——用户展开图表是"研究"场景，但 v1.12 已给图表配了折叠机制控高度，宽度不叠加新变量。
- **结构配对密铺**（v3 定案，取代贪心配平）：按 flow 顺序走——wide 卡独占一行；连续半宽卡两两并排（成对）；段尾落单那张**拉通整行**。flow 与视觉顺序严格双射：预览=落盘=重开，永不洗牌。估算高度**彻底退出布局运算**（v1.13 的 `balancedSplit`/`mergedColumns`/`visibleCards` 高度估算整套删除），"打开面板不洗牌"由模型本身保证而非状态固化——状态更少，约简更强。
- **稳定性三律**：① 面板会话内密铺结果不因数据到齐而重排（结构配对天然满足：新卡只追加，不改变既有相邻关系）；② 一张卡在会话内宽度永不改变（wide 是卡型静态属性，落单拉通只在跨会话生效）；③ 折叠/展开不触发密铺重算，卡在原位长高（v1.13 现状）。

### 数据模型

```swift
// PanelLayout 增加一个可选字段；旧字段保留不删（作业书：新持久化字段必须可选 + 旧档 decodeIfPresent）
nonisolated struct PanelLayout: Codable, Equatable, Sendable {
	var flow: [String]?      // v2 阅读序；nil = 旧档，回退 left+right 拼接
	var left: [String]       // 旧字段保留：读作 flow 的回退来源，写不再使用
	var right: [String]
	var hidden: [String] = []
}
```

读取：`flow ?? (left + right)`。**零迁移脚本、旧档天然兼容**；回滚 = 渲染层还原，数据无损。`hidden`、normalize、托盘机制原样。

## 3. 相对上一版的三个推翻（自检产出）

| 上一版 | 推翻理由 |
|---|---|
| 每卡「跨列/收窄」用户开关 | 违反杠杆归属（Q3）：宽窄是优化不是意图；且 v1.13 刚把编辑模式砍成一只眼睛，加回键位是开倒车 |
| band 显式抽象 + flow/wide 双字段存储 | 密铺 + wide 卡拉平基线，band 结构**免费涌现**，不需要建模；存储只需 flow 一个字段 |
| v1→v2 迁移 + 旧字段废弃 | 可选字段 + 回退读取即可，少一个迁移失败面 |

保留自上一版的核心洞察：华容道的本质不是块型花哨，而是**把要紧的块塞进有界的框**——框=屏幕高度，滑块=拖拽排序，藏块=眼睛。

## 4. 默认阅读序（出厂态，自动模式即此序）

原则（Q1 推论）：**先答现在，后翻档案**。宽卡用 `⟪⟫` 标注：

```
充电协议 | 电池状态        ← 充电时成对；未充电协议缺席 → 电池状态落单拉通
⟪今日用电⟫                 ← 横条图+明细，宽度收益最大
24小时电量 | 健康度趋势     ← 双曲线并排
功耗曲线 | 温度曲线
时段用电 | 用电日历
续航换算 | 洞察
⟪高耗电应用⟫               ← 应用名不再截断
电源事件 | 蓝牙外设
⟪充电记录⟫                 ← 时间戳+充电器+时长，横向最挤的卡
电池身份证                 ← 尾部落单拉通（出厂信息，全宽无害）
```

体检评分继续走头部右侧（v1.11 起的安排）。已存自定义布局的用户不受此表影响。

## 5. 交互（对 v1.13 的增量收窄到最小）

- **拖拽**：把手照旧，落点从"(列, 锚点)"简化为"flow 插入点"——拖过谁就插到谁前面，实时预览密铺让位，松手落盘。跨列思考彻底消失。
- **眼睛**：不变。托盘捞回改为追加到 flow 末尾（v1.13 固定塞右列，放回还得再拖）。
- **重置**：编辑工具条加「重置」一枚——`panelLayout = nil` 回自动模式。v1.11 有过、v1.13 瘦身时丢了的入口，序模型下它同时是"玩坏了的后悔药"。
- 编辑模式仍只有：取消 / 重置 / 完成。不加第三种控制。

## 6. 纯函数与测试（构建门）

新 `PanelFlow`（nonisolated 纯函数，接续 PanelLayoutEditor 的测试资产）：

1. `place(flow, wide, available) → [(id, isWideSlot)]`：结构配对（成对/落单拉通/wide 独行）、隐藏与条件卡跳过、空板；
2. **双射门**：同一 flow 两次 place 结果恒等（预览=落盘=重开的模型保证）；flow 顺序 = 输出展开顺序；
3. `insert/hide/unhide/normalize` flow 版全套（去重、未知 id、缺失追加、幂等）;
4. `CardDropResolver` 改造：落点 → flow 插入点（wide 卡上/下半判定、列带外扩 20pt、空列）；
5. 旧档回退：`{left, right, hidden}` 无 flow 字段解码 → 拼接序正确；
6. 高度抽查（不再做布局运算，只留对账）：v2 默认序的**估算**总高 ≤ v1.13 自动配平估算 + 10%（宽卡不虚耗高度预算，直接对账 v1.12 教训）。

## 7. 风险对照（经验账本 4.1）

- **「改面板布局骨架」正是在案事故类**（ScrollView 空白面板：测试全绿、带病发布）。屏幕录制已授权，按账本规矩执行：分两步合入——第一步只合数据层纯函数+测试（UI 零变化、构建门全绿），第二步合渲染层，**每阶段调试面板（MIAODIAN_DEBUG_OPEN_PANEL）全屏截图验收**（玻璃面板只能全屏截），截图不过不合入。
- **预览几何**：拖动预览沿用"位移以起始 frame 为基准"约定，密铺重排只改让位目标、不回灌落点计算（v1.13 在案约定原样）。
- **回滚**：渲染层一个 revert，配置数据零迁移零破坏。

## 8. 执行计划（本轮实施清单）

| 步 | 内容 | 验收 |
|---|---|---|
| 1 数据层 | `PanelLayout.flow` 可选字段 + `PanelFlow` 纯函数（place/insert/hide/unhide/normalize/旧档回退）+ `CardDropResolver` 平面化改造 | 测试门全绿、零警告；UI 零变化 |
| 2 渲染层 | BatteryPopoverView 分段密铺渲染（删 balancedSplit/mergedColumns/assignedLeft/Right）；拖拽/眼睛/托盘/重置接线；编辑工具条加「重置」 | build.sh 门全绿 |
| 3 视觉验收 | MIAODIAN_DEBUG_OPEN_PANEL 常驻面板分阶段截图：混排板（对+宽+落单拉通）/拖拽让位/托盘/重置 | 截图逐张过目，异常即修；`log show` 零错误 |
| 4 发布 | MARKETING_VERSION→1.14.0；CHANGELOG/README；打包→/Applications→提交→Release | 按更新流程惯例 |

## 9. 自检记录（v2 轮 + v3 复查，逐条过）

v2 轮（设计后、交付前）：

1. **每个决定能否溯源到 Q1–Q4？** flow=意图（Q2），wide/密铺/落单拉通=优化（Q3），两列=物理（Q4），密铺拒绝格子=内容高度可变（Q4）。 ✓
2. **与既有方向冲突？** v1.12 高度治理→§6-6 高度对账；v1.13 编辑瘦身→编辑面净增仅一枚「重置」，否决了宽窄开关。 ✓（自检中修正：初稿曾想保留宽窄开关，Q3 推导后移除）
3. **落单拉通会不会让卡宽跳来跳去？** 会话内三律②保证；跨会话的宽度变化与 v1.13 自动配平换邻居同级，可接受。 ✓
4. **wide 卡虚耗高度？** §6-6 量化对账而非嘴保证。 ✓
5. **新用户空态？** 暖机期条件卡缺席：flow 短→成对或落单拉通，无半宽空洞；twoColumns≥5 阈值行为与 v1.13 一致。 ✓
6. **VoiceOver/AX？** 密铺后渲染顺序 = flow 阅读序，朗读顺序与视觉序天然一致（优于 v1.13 双列交错）。 ✓
7. **拖拽接线遗漏？** 以"grep 无旧 resolve 签名调用点"为完成判据。 ✓
8. **改动量一轮可完？** 纯函数 ~80 行、resolver ~40 行、视图 ~100 行（净删 balancedSplit/mergedColumns）、测试 ~180 行。 ✓

v3 复查（执行前最后一轮，发现并修复 1 个设计级缺陷）：

9. **预览与落盘是否同一真相？** v2 的贪心配平不满足：flow↔视觉非双射，落盘重开可能重配。→ 改纯结构配对，双射成立，§6-2 增双射门。**这是本轮最重要的产出**。 ✓
10. **删除高度估算会不会伤"塞得下"？** 配对质量由默认序设计保证（§4 已按高度相近成对设计）+ §6-6 对账门兜底；用户重排时可实时看到并排效果，自己纠偏。 ✓
11. **旧档升级视觉变化？** 用户已存 left/right 布局升级后按 flow=left+right 重新结构配对，语义序保留、并排组合可能变化——华容道功能三天内 v1.11→v1.13 连续演进，布局尚在流动期，接受并在 CHANGELOG 说明。 ✓

自检结论：**通过**（v2 轮修正 2 处，v3 复查修正 1 处设计级缺陷）。

## 10. 实测验收记录（v1.14.0 交付前）

- 构建门：556 项测试全绿（含行式纯函数 30+ 断言、落点几何 9 例、旧档回退/往返编码），双架构编译通过，零警告。
- 调试面板（MIAODIAN_DEBUG_OPEN_PANEL 常驻）截图验收：行渲染（宽块独行/半宽并排）、把手常驻（每卡 ≡ 胶囊）、编辑工具条（重置/取消/完成）、每卡控制条（眼睛+宽窄）、控制行完整可见均确认。
- 端到端拖拽：验收期间 rows 多次被真实拖拽改写并正确落盘渲染（hourlyDrain 挪顶、batteryInfo+powerEvents 结对、宽块拆对），重启后持久——预览=落盘=重开得到真实使用验证。
- 连拍两帧逐像素一致：无洗牌、无漂移。日志零错误。
- v1.13.0 拖拽坏死缺陷（把手只挂拖动中的卡，常态无处可抓）已随本版修复。
