# 妙电经验账本（append-only，循环的记忆）

## 4.1 教训（三行式，if-then 可执行）

[技术坑] 2026-08-30 ScrollView 渲染空白事故
现象：MenuBarExtra(.window) 面板卡片区包 ScrollView + frame(maxHeight) 后整块渲染空白，测试全绿却带病发布。
根因：菜单栏弹窗是特殊 NSPanel 宿主，滚动容器的高度协商与常规窗口不同；且当时无视觉验证通道。
下次：遇到〈改面板布局骨架〉先〈只做加法不改结构；无屏幕录制授权就不动布局，动了必须截图验收〉。

[技术坑] 2026-09-04 shell 引号截断
现象：commit -m 内嵌中文引号把消息截断，后半段被当命令执行。
根因：双引号字符串里再放双引号。
下次：遇到〈提交信息/notes 含引号或换行〉先〈git commit -F /tmp/msg.txt、gh release --notes-file〉。

[技术坑] 2026-09-04 上游标签撞版本线
现象：打 v1.2.0 时 fatal: tag already exists——是 fetch upstream 带下来的上游标签。
根因：fork 与上游共用语义化版本号。
下次：遇到〈任何 fetch upstream〉先〈带 --no-tags；发布线只用本仓库 tag〉。

[技术坑] 2026-09-04 nonisolated 函数引用 static let
现象：Swift 6 审计报 main actor-isolated static property 不能从 nonisolated 上下文引用。
根因：-default-isolation MainActor 下 static let 默认 MainActor 隔离。
下次：遇到〈纯函数要引用类型常量〉先〈该常量显式标 nonisolated〉。

[方法论] 2026-09-04/05 静默失败透镜最高产
现象：两夜里"导入失败静默/导出失败静默/自愈路径零测试"全部出自静默失败透镜。
根因：guard else return / try? 是"沉默的谎言"高发区。
下次：遇到〈新一轮出题〉先〈优先扫一遍新增或改动过的 catch/guard/try? 分支〉。

[方法论] 2026-09-04 冷启动功能写两周后验证法
现象：热叠加/打架检测/时段归因都需几天数据才出现，当夜无法证伪。
根因：洞察依赖数据积累。
下次：遇到〈新增依赖历史积累的洞察〉先〈报告写"两周后如何验证"，否则降级队列〉。

[产品判断] 2026-09-05 "审计后不改"也是合格产出
现象：周报×月报同日双发，判定收益不足不改，以断言锁死现状。
根因：质量门槛同样约束"修"与"不修"。
下次：遇到〈机制冲突类追问〉先〈允许以"有证据的不改+断言锁死"收尾，不硬凑改动〉。

## 4.2 无恙清单（查过了，防重复审计）

- [时间] 时钟回拨/未来标记 → v1.6.3 已修（digestSendAllowed/gaugeCooldownActive/日键升序）
- [状态机边界] 月度折叠停滞 → v1.6.7 已修（跳过单点月）
- [静默失败] 蓝牙自愈路径 → v1.6.6 已补测（canStartBluetoothRefresh）
- [机制冲突] 周报×月报同日双发 → v1.6.8 审计判定可接受，4 断言锁死
- [数据流] 充电器峰值功率/电芯压差快照 → v1.6.4 已露出/标注
- [用户认知] 体检估计项标注 → v1.6.5 已修（estimatedInputs）
- [数据完整性] 换电池污染趋势 → v1.3.0 已修（序列号边界+趋势过滤）
- [静默失败] 存档导入/导出、报告/CSV/卡片导出失败 → v1.6.1/v1.6.2 已修

## 4.3 直觉校准（每轮先预测后对照）

（本夜起记录）

## 4.4 自我进化（每夜收尾三问）

（本夜收尾填写）
