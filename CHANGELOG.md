## v2.9.2 — 2026-09-23（睡眠段时长与高电量驻留归因）

- **透镜**：C 丢失·时间（睡眠唤醒）。追问：合盖那 8 小时，电池传记里去哪了？
- **缺陷**：`acSeconds / batterySeconds / soc80to90Seconds / soc90to100Seconds` 只在**相邻连续帧**（≤30 秒）之间累加。合盖期间一帧都没有，整夜时长被全部丢弃——实测某日只记到 6.1h（应有 24h），而那一夜 11.9h 睡眠（90%→86%）连驻留一起没算。
- **后果**：插电占比、高电量驻留洞察与周报对比、月报日均、依赖 `acShare` 门槛的结论**系统性偏低**，且用户完全无从察觉。
- **修复**：`willSleep→didWake` 区间在醒来结算时按自然日拆分补记时长与两档驻留：
  - `sleepSegmentParts`：跨午夜按注入日历拆段，段端点电量按时间线性插值；时钟回拨（wake ≤ sleep）不产生片段
  - `dwellShareAbove`：单调线性路径下「电量 ≥ 阈值」时间占比的闭式解，90+ 与 80–90 两档不重不漏
  - `creditingSleepTime`：只补时长/驻留，**不动** `drainedPercent/chargedPercent`（夜间掉电不冒充瞬时用电，既有语义保持）；那天没有日行则跳过该段（不凭空造天，护住「记录 N 天」与月报日均分母）
  - 供电来源在**合盖那一刻**定格（醒来后只剩「现在的」状态）；`process` 里睡眠结算移到当日行建好之后，否则跨午夜那一觉的醒来日时长会被缺行跳过
  - 时长归因不受「≥20 分钟小憩」门约束——那道门只管睡眠掉电记录与告警
- **成本**：零新增定时器/采样/defaults 键；一次性（结算即清 `pendingWake`，重启与导入不重放）→ 归档三面无需改动
- **已知边界**：睡眠中拔插无法观测，整段按合盖时电源归因；结算用醒来时刻为段尾（醒来后未刷新的几秒不进归因）
- **门**：测试 **1135** 全绿零警告（本机）/ `TZ=UTC` 全绿 / Swift 6 全量审计零错误零警告 / 变异检验 3 条（插电↔电池槽位写反、驻留占比写反、日键不绑 `calendar.timeZone`）各自必红 / 异时区门禁含 UTC+14 注入日历

## v2.9.1 — 2026-09-22（信任补丁：静默失败 / 数据完整性 / 口径诚实）

十面透镜全量排查后的一轮修复。第一性排序：复利数据 → 诚实口径 → 完整告知。

- **历史完整性（复利数据）**
  - `makeArchive()` 补传 `sleepDrainHistory`——换机逃生舱不再丢 E1 合盖史；导入回执含睡眠条数
  - 空/残档导入拒绝：`isEmptyHistory` 先挡再问，不再「导入完成，存档为空」清光现网历史
  - `insertingDailyUsage` 封顶改「删最旧非今天」：超长档 + 今天落中部不再 `removeSubrange` 越界 trap
  - 再次合盖前先结算 `pendingWake`：醒后未刷新就睡，上一觉掉电不再静默丢失
- **提醒投递诚实**
  - 周报/月报/电量计校准：`send` 成功才落标记（免打扰不再永久吞掉本期；校准不再空转 30 天）
  - 睡眠掉电：新鲜度窗 10min→**8h** + `evaluate()` 周期重试，免打扰结束后可补发（注释不再说谎）
  - 健康度里程碑一跳跨多档报全（92→81 交代 90/85/80）；投递失败不推进基准
  - 循环里程碑同理报全（700→1100 提及 800/1000）；健康骤降改吃 `trendHealthSamples`（换电池边界）
- **口径诚实**
  - 掉电窗口返回**真实跨度**，刚拔电 10 分钟不再说「最近一小时」
  - 存放文案与门控带一致（45–65%），不再写「约 50–60%」
  - 「本周累计」改「近 7 日（面板打开时采样）」——不冒充全周能耗归因
  - 报告充电器走别名查找（降档归并后不再整段消失）；报告体检驻留用「今天」而非 `last`
- **门**：测试 **1105** 全绿零警告 / Swift 6 零错误（MacOSX26.5）/ 变异检验含封顶越界与多档里程碑

## v2.9.0 — 2026-09-21（E4 动态续航精调）

- **透镜**：数据完整性 + 自身成本 + 可溯源（估计质量）
- **窗口自适应**：`DrainRateEstimator.adaptivePercentPerHour`——稳定用满 60 分钟窗；负载突变时切近 15 分钟短窗。短窗要求 **至少掉电 2%** 才参与判定，并另有相对+绝对阈值；低基线下 1% 量化台阶不假突变（对抗审查 D4 已补负例）
- **本机强度校准**：`RuntimeScenarioCalibration`——从既有 `DailyUsage` 推日放电强度（%/h），近期中位数/基线中位数夹紧 `[0.80,1.25]`，再夹紧场景系数；样本不足维持出厂系数
- **诚实口径**：**不是** per-scenario 回归（历史无场景标签，不编造）；卡片注明强度因子并写明「场景系数已夹紧」
- **窗口文案同源（对抗审查 D8）**：告警/设置/掉电行按 `windowSeconds` 写「最近一小时」或「近 N 分钟」，不再短窗谎称一小时
- **能量悖论审计（不做）**：亮度/网络因子——持续读显示/网络成本过高，明确拒绝
- **UI**：续航卡短窗写「近 N 分钟」；微调口径一行；有口径时配平高度 +16；不改布局骨架
- **无新 defaults 键**；旧 `DrainRateEstimate` 构造兼容（`windowSeconds` 可选）
- **门**：测试全绿零警告 / Swift 6 零错误 / 变异检验：禁用突变切换→短窗断言红；去掉系数夹紧→断言红
- **两周验证（W1）**：预估 vs 实测掉电偏差目标 ±10%；负载变化后应更快跟上

## v2.8.1 — 2026-09-21（dayKeys 时区跟随 + v2.8.0 发布链收尾）

- **发现**：v2.8.0 的 `DwellTracking.dayKeys` 用 `DateFormatter` 生成窗口键，却未绑定注入 `calendar.timeZone`——formatter 默认吃系统时区，与注入日历不一致时（CI=UTC、测试注入 Asia/Shanghai）近7日/近30日窗口键整体错位一天，CSV「能耗聚合」断言在 CI 必红
- **根因**：与周报窗口谎言同族——窗口计算用了注入 calendar，字符串化却没用同一时区；`UsageCalendarLayout.dayKey` / `monthKeyString` 已绑时区，`dayKeys` 是漏网点
- **修复**：`dayKeys` 改走 `UsageCalendarLayout.dayKey`（calendar.timeZone 同源）；`UsageCalendarLayout` 标 `nonisolated`（可被窗口纯函数调用）
- **测试**：新增 UTC+14 注入日历断言（formatter 不绑 calendar 时本地也必红）；CSV 近7日聚合断言在 UTC 下复现并消失
- **门**：测试本机 **1054** / CI 时区 **1055** 全绿零警告 / Swift 6 零错误（MacOSX26.5）/ 版本 2.8.1
- **说明**：v2.8.0 功能本体不变；2.8.0 的远端 tag/Release 未发布（CI 红），本版一次性补齐 E3 发布链

## v2.8.0 — 2026-09-18（E3 能耗报告聚合 + 周报窗口诚实修复）

- **发现（数据完整性透镜）**：`weeklyDigestBody` 对**全部** `dailyHistory` 求和却写「本周用电」——把装上以来说成本周，属传记失真级信任事故，按作业书优先修
- **修复**：周报强制 `due:`，只统计截止 due 的近 7 日窗（与 `DwellTracking.dayKeys` 同源）；窗外电量不得并入
- **修复**：月报日均改为「记录 N 天，日均 X%」——按有效样本日摊，不用日历天数把缺记录的日子摊成 0
- **新增** `EnergyAggregation`：窗口聚合 / 峰值日 / 近窗对比 / 诚实摘要行；有效样本=有充放电或通电≥30min
- **报告**：【最近用电】前置近7日+本月聚合行（注明日均口径与「无月级电量曲线」）
- **CSV**：新增 `# 能耗聚合` 表；`csv()` 可注入 `now` 便于测试
- **门**：测试 **1051** 全绿 / Swift 6 零错误（MacOSX26.5）/ 构建 2.8.0 / DMG 3.2M
- 直觉校准：预测「周报窗口谎言」会在 E3 设计时挖出 → 读代码时即命中；预测月报日历天摊薄低估画像 → 测试原文「日均用电 2%」实锤

## v2.7.0 — 2026-09-18（循环④ H3 健康异常·诚实子集）

- `HealthAnomalyDetector`：30 天健康度降幅 ≥ 阈值（默认 5 点）+ 循环里程碑（默认 800/1000）
- **不做**持续高温新类型：现有温度画像无法诚实支撑「连续 N 天」，避免编造
- 复用 `.alertHealthMilestone` 开关族；与 90/85/80 里程碑分 notification id；降幅按天去重；循环 seen 成功才推进
- 计划 `docs/compose/plan-h3-health-anomaly.md`；测试 **1026**

## v2.6.0 — 2026-09-18（E1 睡眠耗电分析）

- `SleepDrainAnalytics`：近 14 天合盖记录聚合（≥1h 且掉电≥1%），avg/max %/h + 元凶频次排行
- 建议三档（偏快/略偏快/大致正常）；偏快时点名元凶并写明「只提示，不会结束进程」
- 历史：`sleepDrainHistory` 滚动 20 条入档（旧档兼容）；面板睡眠行悬停给聚合句
- 计划 `docs/compose/plan-e1-sleep-drain.md`；测试 **1016**

## v2.5.0 — 2026-09-18（H2 老化趋势置信表达）

- 外推到 80%：强制「估计」+ 相对置信区间（跨度越短区间越宽：14–30 天 ±45%，30–90 ±30%，>90 ±20%）
- 双锚点：容量斜率与循环当量（标称 0.02%/循环）各半；缺循环增量则仅容量并悬停说明
- 健康趋势卡寿命句改 `HealthAgingProjection.displayLine`；help 含方法与相对区间
- 门：测试 **1002**；计划 `docs/compose/plan-h2-aging-ci.md` 先自审后实现

## v2.4.0 — 2026-09-18（C4/C5 + 驻留洞察闭环 + H1 口径 + R9）

- C4 涓流：洞察「系统涓流中」；充电曲线悬停标注阶段（恒流/恒压/涓流）
- C5 存放：洞察一条（插电≥80% 提存放带；电池模式落在 45–65% 正向确认）；不新建高卡
- C3 闭环：驻留对比进洞察链（下降正向 / 偏高提示办公预设）；周报附驻留日均对比句
- H1：健康行 help 直读口径 + 与系统可能差 1–2% + 估计项指向体检
- R9：预览 harness `@preconcurrency` 消 IsolatedConformances
- 洞察优先级：习惯 → 系统暂缓 → 热 → 驻留 → 慢充 → 存放 → 涓流
- 门：测试 **978** 全绿；交付前自检

## v2.3.0 — 2026-09-18（C3 驻留效果追踪）

- 日摘要驻留行扩展（不新建卡）：今日驻留 + 近7日合计/日均 + 与前7日对比（下降/上升/持平）
- 纯函数 `DwellTracking`：周聚合有效日≥3 才下结论；对比窗口两侧各≥2 才给句；不足则沉默（第一次透镜）
- 测试 943→956；效果趋势需你用机两周后再看（作业书约定）

## v1.17.2 — 2026-09-08

- 拖拽动效分相：抓起/放下有"提起一张卡"的过渡（放大与阴影走弹簧）；拖动中卡片 1:1 直跟指针（不再弹簧慢半拍）；松手从手指位置弹回槽位落定
- 把手浮层位置与卡片让位同拍：入场与重排时把手不再滞后闪跳
- 编辑布局模式下隐藏拖拽把手浮层（不再叠画在控制条上）
- 设置页「暂不可用」行移除恒灰假开关（点了没反应的假按钮，VoiceOver 误读）——只留卡名与缺席原因，无障碍合并朗读「卡名，暂不可用：原因」

## v1.17.1 — 2026-09-08

- 修复面板拖拽卡死：拖动中卡片悬停半空不再跟手（根因：把手长在卡片结构内，预览重排改行/段身份时被拖卡子树连树销毁，进行中的拖拽手势随之中断）
- 拖拽把手浮层化：把手单独平铺一层（身份恒等于卡自身，位置实时跟随卡片），预览重排只改坐标、手势全程存活；面板卡片渲染零改动
- 防误伤双保险：拖动中按到别的把手不再互相污染状态；关面板自动清残留拖拽态

## v1.17.0 — 2026-09-07

- 修复 v1.16.0 后悔药缺陷：自动模式（未自定义布局）下应用预设后，「撤销上次布局改动」按钮不出现——新增 undoWasAuto 记录上一状态是否为自动模式，撤销可回到自动配平；旧配置缺字段解码为 false，无缝升级
- 「恢复默认排序」更名「恢复默认布局」，名副其实：行序回出厂，且已隐藏的卡全部回来（快照+清空合并为原子操作，任何破坏路径都先留后悔药）
- 卡片管理列表补全库存（稳定清单）：面板缺席的卡不再从设置里消失——新增「暂不可用」区逐卡说明原因（功能开关未开→指出开关位置；缺数据→说还差什么），「已隐藏」区不再按数据过滤（隐藏的卡永远可见，恢复通道永不失效）
- 预设确认弹窗显示当前显示中的卡数；测试 592→616 项全绿

## v1.16.0 — 2026-09-07

- 「卡片管理」列表改为面板的文字影子：复用面板 renderSegments 渲染，一行 = 面板一行——并排的两张卡在同一行里各带自己的控件（中间分隔线对应面板半宽分界），打开分区一眼认出自己的面板；不再用平铺 14 行 + 上下移按钮的线性列表
- 每卡显示开关改名「面板显示」，与功能分组开关的语义切开；分区顶部提示「调位置在面板拖卡片把手，这里管批量开关与预设」
- 后悔药：应用预设/恢复默认排序前自动把当前布局留存为 lastCustomLayout，出现「撤销上次布局改动」一键写回（快照/撤销均幂等）；「恢复默认排序」补上确认弹窗，与预设对称
- 上/下移按钮降级为行内菜单的「上移/下移一位」（阅读序兜底，键盘与 VoiceOver 可达），排位置的空间任务只属于面板拖拽把手
- 旧配置无缝升级：无 lastCustomLayout 字段解码为 nil（无上一版可撤销），不丢任何状态；测试 581→592 + 3 组变异检验（存错槽位/不写回/不清空）

## v1.9.0 — 2026-09-06

- 充电器识别 v2：同一只头多口分功率/协议降档（如 100W 掉到 70W）不再裂成两个档案——统计连线、速度对比共享样本，当次功率进观察记录（悬停可见"观察功率档：70W/100W"）；历史遗留的降档档案读档时自动并回母档案

## v1.9.1 — 2026-09-06

- 可读性保证可证明化：新增 WCAG 对比度证明纯函数并纳入测试门——表面 token × 文字色 × 亮暗外观 × 降低透明度，全壁纸亮度域最坏对比度逐项断言（浅色 primary 14.1:1、深色标签 5.2:1 等全表达标）
- 「降低透明度」显式分支：不透明纯色底替代玻璃（不再依赖系统回退行为）
- 玻璃上标签文字加深（primary 85% 替代 secondary）：透底最坏对比度 2.7→4.5+ 达 AA
- 深色外观亮度地板按证明调校（0.35→0.78）：亮壁纸下白字对比度 2.7→6.3
- 外壳材质语汇：顶边镜面高光+底部内阴影，文字区 12pt 内缩避开亮边

## v1.9.2 — 2026-09-06

- 深色外观对比度达标收尾：亮度地板 0.78→0.81，深色外观关键数字最坏对比度 6.34→7.01 达 AAA（证明表全绿：浅色 14.1/11.7、深色 7.01/5.66、降低透明度 ≥7）
- 面板定位屏幕锚定收尾：与 iBar 图标位置解耦（实测图标被收纳时锚点漂移），贴菜单栏下缘+右缘留边
- 三辅助开关实测：降低透明度 ✓（不透明底+文字全清晰）、增强对比度 ✓（发丝线加强可见）；减少动态截图待补（行为验证：动画直接终态，代码分支已实现）

## v1.10.0 — 2026-09-06

- 面板高度治理：温度曲线/功耗曲线/健康度趋势三张图表卡默认折叠（全展开时面板超出屏幕可用高度，控制行被 Dock 遮挡），面板高度 1079→973；用户展开/折叠的选择照常记忆
- 卡片语义分簇重排：充电中 → 电池健康 → 用电行为 → 外设与事件，同簇相邻扫读不跳
- 洞察卡多行化：不再只挑一条说——习惯规律/系统充电打架/热叠加/充电器观察按优先级排列成一张洞察卡

## v1.11.0 — 2026-09-06

- 华容道布局：控制行新增「编辑布局」——进入后每张卡片右上角出现控制条，上/下移、跨列移动、隐藏（藏进底部托盘可捞回），点「完成」布局落盘，重启不丢
- 布局持久化到配置（panelLayout：左/右/隐藏三表），正常模式严格按用户排布渲染；「恢复默认排序」一键回到自动配平
- 首次进入编辑模式以当前排布为起点；编辑中面板顶部有提示条与「取消/完成」

## v1.12.0 — 2026-09-06

- 华容道布局编辑模式：控制行新增「编辑布局」——进入后每张卡片可长按拖拽到任意位置，松手自动补位；点「眼睛」隐藏不想看的卡片（藏进底部托盘可捞回）；点「完成」布局落盘
- 拖拽引擎：长按 0.35s 激活 + DragGesture 跟手 + 实时让位预览（其他卡片自动补位）+ 松手插入；`CardDropResolver` 纯函数计算落点（纳入测试门）
- 布局持久化：panelLayout（左/右/隐藏三表）存入配置，重启不丢；恢复默认排序一键回自动配平
- 面板定位屏幕锚定：与 iBar 图标位置解耦（实测图标被收纳时锚点漂移，面板漂到菜单栏下 50pt）
- 面板高度治理：温度/功耗/健康度趋势三张图表卡默认折叠，面板高度 1079→973pt，控制行不再被 Dock 遮挡
- 可读性：36 处标签换 primary 85%（透底最坏 ≥4.5 AA）；深色外观地板 0.81（关键数字 ≥7.0 AAA）；外壳顶边镜面高光+底部内阴影

## v1.13.0 — 2026-09-06

- 华容道拖拽：每张卡片右上角常驻拖拽把手（六点胶囊），按住即拖、松手落位——与卡片内按钮零手势竞争（修复长按被内容吃掉的问题）
- 拖动中其他卡片实时让位（弹簧动画补位），落点处就地插入
- 隐藏卡片：编辑布局瘦身为纯隐藏管理（眼睛开关+托盘捞回）
- 面板点击外部自动关闭（全局鼠标监视器，不依赖焦点状态）
- 面板定位屏幕锚定，不再随 iBar 图标收纳漂移

## v1.14.0 — 2026-09-07

- 华容网格 v4（显式行存储）：自定义布局以「行」为单位——宽卡独占整行、两张半宽卡并排，渲染即存储，拖拽预览=落盘=重开，永不错位
- 修复 v1.13.0 拖拽坏死：把手此前只挂在拖动中的卡上，常态无处可抓；现在每张卡右上角常驻六点把手，按住即拖
- 编辑模式每卡新增「宽窄」键：半宽拉宽为独占整行、宽块收窄并回相邻行——横向内容（充电记录的应用时间线、高耗电应用名、今日用电明细）不再被半宽截断
- 落点几何重写：被拖卡自身不再参与锚定（修复"插到自己身上"误判），拖动跟手以起始位置为固定基准
- 托盘捞回改为追加板尾单行（不再固定塞右列）；编辑工具条新增「重置」一键回自动模式；未改动时「完成」不落盘（防"存一次长高一截"）
- 自动模式保持 v1.13 贪心双列不变——高度门实测行式对齐密铺出厂态超屏 33%，列式是物理最优；新布局引擎只作用于自定义布局
- 旧档无缝升级：v1.11–v1.13 的左右列布局按索引对齐成行，重启即用，重置可一键回出厂

## v1.15.0 — 2026-09-07

- 设置窗口新增「卡片管理」分区（自定义布局主干编辑器）：每卡一行——显示开关、上/下移、默认折叠（11 张图表卡），与面板编辑模式写同一份 PanelLayout 契约，稳定窗口原生无障碍
- 预设四款：默认（自动配平）/极简/充电党/数据控——只在资格集（当前有数据的卡）内生效，绝不强行显示空卡；覆盖现有自定义布局前确认一次
- 「恢复默认排序」一键回自动配平；已隐藏且有数据的卡列出「显示」按钮原位捞回
- 资格判定抽为可注入纯函数（eligibleCards + 数据在场事实），新增「默认布局==今日顺序」回归断言守住面板阅读序；测试 556→581


# 更新日志

妙电（MiaoDian）—— ChargeMonitor 汉化增强版。版本号遵循语义化，日期为发布时间。

## v1.8.0 — 2026-09-06

### 液态玻璃重塑（可读性优先）

- 面板整体迁移 macOS 26 Liquid Glass：外壳玻璃板+自适应亮度地板（浅/深/花三壁纸实测黑字全部 ≥AA，关键数值 AAA），卡片降为玻璃上发丝分区，控件/徽章/仪表为可交互玻璃
- 面板宿主替换为菜单栏图标+自建透明面板：玻璃真正采样桌面；面板贴靠菜单栏下缘；设置行走 SettingsLink（旧选择器已被系统拒绝）
- 应用图标重绘为液态玻璃电池舱（CoreGraphics 生成，10 尺寸 icns）
- 兼容性：部署目标保持 macOS 15.0（26 走玻璃、15–25 原质感降级）；「减少动态」「增加对比度」辅助开关接入；VoiceOver 标签原样保留
- 能量红线：面板关闭稳态 CPU 0.0%
- 开发者：新增预览 harness、对比度实测工具、程序化开面板调试通道（docs/glass/ 有前后对比截图）

## v1.6.14 — 2026-09-05
- 充电记录行显示是哪只充电器充的（认不出/旧记录安静省略）

## v1.6.13 — 2026-09-05
- 导入历史存档成功给分类计数回执——成功也值得一次开口

## v1.6.12 — 2026-09-05
- 电池更换边界（序列号+更换时刻）随全量存档往返，恢复后趋势不再混两块电池

## v1.6.11 — 2026-09-05
- 寿命预测悬停披露线性外推方法与不确定性，预言降格为估计

## v1.6.10 — 2026-09-05
- 蓝牙轮询面板关闭时降频 30s→120s，省电工具不再自己偷电

## v1.6.9 — 2026-09-05
- 高耗电卡暖机期显示"正在积累样本"，不再对新用户谎报"没有耗电大户"

## v1.6.8 — 2026-09-05
- 周报×月报同日双发审计：判定可接受并补 4 条交互断言；周报调度补 calendar 注入

## v1.6.7 — 2026-09-05
- 月度折叠跳过单点月：导入异常存档不再让 90 天封顶停滞

## v1.6.6 — 2026-09-05
- 蓝牙轮询卡死自愈路径抽纯函数补测（4 条断言锁死）

## v1.6.5 — 2026-09-05
- 体检评分如实标注估计项：冷启动的循环/温度/驻留中性估计不再沉默

## v1.6.4 — 2026-09-05
- 充电器峰值协商功率露出（诊断线材用）；电芯压差标注为开机快照

## v1.6.3 — 2026-09-05
- 时钟异常下游标键降级：日键升序插入防错删；未来标记失效，小结不再被永久静默

## v1.6.2 — 2026-09-04
- 静默失败审计收尾：导出报告/CSV/体检卡片失败时弹错误反馈，不再只进日志

## v1.6.1 — 2026-09-04
- 存档导入/导出失败不再静默：损坏或高版本文件弹「无法解析」，取消保持安静

## v1.6.0 — 2026-09-04
- 应用耗电时段归因：本周累计排行新增「集中在 X–Y 点」标注（24 小时活跃分布，3 小时窗口 ≥60% 才给结论）

## v1.5.0 — 2026-09-04
- 保养提醒 vs 系统优化充电打架检测：识别系统在保养线附近反复暂停的指纹，提示二选一，只提示不干预

## v1.4.0 — 2026-09-04
- 温度 × 时段用电交叉洞察：用电高峰叠着电池最热时段时提示散热；新增持久化的每小时温度峰值画像

## v1.3.0 — 2026-09-04
- 电池更换自动识别：序列号变化记事件边界，健康趋势/寿命预测只看更换后样本，传记不被换电池污染
- 充电会话关联充电器：曲线窗口显示「是谁充的」，速度对比优先与同一只充电器比
- 睡眠元凶持久化：阻止睡眠的进程名留档，面板与报告可复述

## v1.2.0 — 2026-09-04
- 充电器身份键加入 PD 档位签名与无线标记：两只同瓦数的不同充电器自动分开建档，慢充统计不再被互相拉平

## v1.1.9 — 2026-09-04
- 第三方充电器用户命名：系统认不出的头可在设置里认领名字，面板/报告/洞察同步说人话

## v1.1.8 — 2026-09-03
- 视觉验收修正：体检说明文案跟随驻留评分；电流近零读数不带符号

## v1.1.7 — 2026-09-02
- 通知可交互：保养提醒「延后 30 分钟」、慢充「复制诊断信息」；面板打开时提醒不再被前台吞掉
- App Intents「电池速览」：Shortcuts / Spotlight 直接查询电池状态
- 优化充电暂停不再拆散充电会话

## v1.1.6 — 2026-09-01
- 历史存档导出/导入（换机/重装数据逃生舱）；健康样本月度折叠永久保留
- 库仑计数估算；高电量驻留画像进体检评分；睡眠掉电通知点名阻止睡眠的进程

## v1.1.5 — 2026-08-30
- 面板卡片区滚动兜底回滚修复；设置窗口行内说明与搜索；文案与反馈一致性批量修正

## v1.1.0 — 2026-08-28
- 首个开源版本：universal binary（arm64 + x86_64）、最低 macOS 15；能耗采样移出主线程；插拔电事件驱动；
  通知时效性分级；免打扰时段可配置；352 项测试与 CI；跨睡眠电量归因修复、历史存档防护等
## v1.17.3 — 2026-09-08

- 拖拽重排丝滑动效：卡片让位从"跳格子"改为**弹簧滑行**——卡片渲染从行栈（以段落内容为身份，重排即销毁重建、新视图凭空出现、无从动画）改为自定义密铺 Layout（卡片以自身为身份平铺，重排=坐标重算+全员滑行）
- 排版像素契约不变：行距 8 / 卡距 10 / 半宽等分 / 行高=行内最高卡顶对齐
- 密铺几何下沉为纯函数 masonryPlan，测试锁定
## v1.18.0 — 2026-09-08

- 修复「电池体检」的面板显示开关完全无效果：宽面板把体检评分搬进头部角标（既有设计），但角标此前只听功能开关、不听「面板显示」——设置里关掉纹丝不动；现在隐藏体检卡头部角标同步消失（自动模式/旧档不受影响）
- 面板拖拽纳入后悔药：拖动落盘前自动快照，拖乱了几下可在设置 → 卡片管理点「撤销上次布局改动」一键找回（此前撤销只覆盖预设与恢复默认）
## v1.18.5 — 2026-09-08

- 修复时区旅行丢「今日小结」：向西跨日界线后今天的日期键成为全表最旧，90 天封顶裁剪把它当作"最旧的一天"裁掉——当天数据全部静默丢弃，今日小结当天起永久缺失；现在封顶时永远保住今天、改裁最老的旧天（普通封顶行为不变）
## v1.18.6 — 2026-09-08

- 动效丝滑扫荡：五处瞬跳全部改为弹簧/转场——图表卡折叠展开（点「∨」不再硬切）、进出编辑布局模式（提示条与按钮组淡入淡出+滑入滑出）、抓起时把手隐没/松手浮现（0.15s 淡变）、编辑模式隐藏与宽窄切换（卡片滑行让位+托盘插行跟随）、托盘行插拔转场；「减少动态效果」开启时全部直给终态
## v1.18.7 — 2026-09-08

- 面板抗透底：修复白底黑字窗口贴在面板后面时，背景文字以「幽灵」穿透卡片内容区（温度曲线绘图区最明显）——浅色地板 0.4→0.68 白 + 卡片填充加强为第二道衰减，穿透摆幅 28.7%→11.8%（低于人眼可辨读下限）；文字对比度只升不降（primary 17.3 AAA）
- 新增「干扰度」可读性证明断言（卡片内容区背景穿透摆幅 ≤0.12，浅/深双证）——堵住「只证文字对比、不证背景穿透」的证明盲区，纳入测试门
## v1.18.8 — 2026-09-08

- 修复 v1.18.7 抗透底导致的发暗：卡片填充误用黑 tint（26% 黑纱让浅色卡片表面亮度 0.85→0.70）——改用白纱，同等穿透衰减（抗透底效果不变）且卡片恢复透亮
- 证明测试新增「卡片不发暗」不变式（浅色卡片表面最暗处 ≥0.85），防同类回归
## v1.19.0 — 2026-09-08

- 点通知直接看详情：提醒通知（充满/低电量/保养/慢充/耗电异常）的**点按本体**此前是空操作——现在点击自动打开面板（已打开则不动）；「延后 30 分钟」「复制诊断信息」按钮行为不变
## v1.19.1 — 2026-09-08

- 修复夜间免打扰吞提醒：全部提醒通知（充满/保养/慢充/温度骤升/健康里程碑等 12 类）此前都是「先落已通知标记、再发送」——免打扰时段发送被静默跳过时标记仍说已通知，**免打扰结束也不补发**。夜间充电过夜的用户早上醒来提醒永远丢失（健康度里程碑为一次性事件，丢失最致命）
- 现在免打扰时段被跳过的提醒，在免打扰结束、条件仍成立时自动补发；紧急类（低电量）走时效性通知旁路不受影响；「延后 30 分钟」语义不变
## v1.19.2 — 2026-09-08

- 「见过 N 次」口径审计与锁定：N 只在拔插超过 30 分钟的重连时计次（应用重启/睡眠唤醒不虚增），补边界断言锁定；设置充电器列表新增悬停说明，口径答案放在数字旁
## v1.19.3 — 2026-09-08

- 通透回调：面板太白的修正——壳层地板 0.68→0.5（壁纸重新透出，玻璃活了），卡片白纱 0.26→0.29（内容区抗透底由白纱承担）；卡片内容区背景穿透摆幅 0.177 受控，亮背景下卡面 ≥0.90 不发灰，黑字 16.9 AAA，深色外观零变化
- 证明断言重构为三道锁：通透不变式（卡面 ≥ 同位置裸壳）、亮面下限（≥0.90）、卡面黑字 AAA
## v1.20.0 — 2026-09-08

- 面板材质三选一（设置 → 通用 → 面板材质）：**通透**（壁纸清晰透出，玻璃感最强）/ **均衡**（默认，背景轮廓可辨，升级用户观感零变化）/ **厚重**（接近实色，杂乱背景最稳）；更改即时生效
- 三档 × 双外观全部经可读性证明表锁定（卡面黑字 AAA、亮面 ≥0.88、白纱不压暗、穿透摆幅按档阈值 0.24/0.18/0.10）——把「透明 vs 可读」的权衡交给用户，底线由证明兜住
- 深色外观三档同参（白字 AAA 要求深色面板近不透明）；「降低透明度」系统开关优先级高于档位

## v1.21.0 — 2026-09-08

- 视觉氛围感第一期（纯逻辑层，UI 零改动）：新增 `BatteryVisualResolver` 状态解析器——把电量/充电/温度解析成填充通道四态（充电 > 已充满 > 低电 > 平静）与温度警示急促度，为后续波浪/呼吸/边框动效铺数据底座
- 动效诚实原则全落测试（24 条变异检验断言）：波速随瞬时充电功率连续加快（10W 慢 2.4s → 60W 快 1.2s，超界封顶；功率缺失按最慢，从容不吓人）；温度急促度阈值 1.0 → +8°C 封顶 2.0 线性；热警示带 1°C 迟滞（阈值附近抖动不闪烁）
- 口径不另造：插电低电不判危机（与头部 isLowBattery、菜单栏变色同一套语言）；数据缺失一律退安静（缺数据 ≠ 有热度/在狂奔）；零新增采样（功率复用 IOKit 已有字段）

## v1.22.0 — 2026-09-08

- 头部圆环氛围层第一期：**充电中**显示绿色波浪液面——液位 = 真实电量占比，波速随瞬时充电功率连续加快（10W 从容 → 60W 急促），主波 + 反向错相淡波两层视差；**正常/已充满**为极淡静息渐变，无常驻动效
- 成本纪律照旧：Canvas 纯几何位移、零模糊、12fps；「减少动态效果」开启时波浪退化为静止波面（保留液位与语义色，只让步动效）
- 低电状态本版先落橙色底色，呼吸动效 v1.23.0 接管

## v1.23.0 — 2026-09-08

- 头部圆环氛围层第二期（警示通道）：**高温**时头部外缘橙色预警边框——阈值与迟滞同高温提醒的用户设置，脉冲节奏随温度连续加快（40°C 1.6s/拍 → +8°C 封顶 0.8s/拍），1°C 迟滞保证阈值附近抖动不闪烁；**低电**时圆环内底色暖橙→红渐变 + 轻呼吸（2.2s，比充电光点更慢更沉——提醒而非催促），文字永不参与呼吸
- 高温与低电/充电可叠加显示（充电生热是最危险的真实组合），互不吞并；「减少动态效果」全部退化静息
- 附临时调试注入口 `--miao-visual=<charging|low|hot|hot-charging>`（目检四态用，v2.0.0 移除）

## v1.24.0 — 2026-09-08

- 面板材质三档重设计（设置 → 通用 → 面板材质）：**通透**=原生 clear 玻璃（壁纸清晰透出，玻璃感最强）/ **均衡**=原生 regular 玻璃（系统默认浓度）/ **厚重**=regular 玻璃加浓 tint（接近实色）——三档映射 Apple 材质系统的真实选项，不再是同一块玻璃上的白纱浓淡（v1.20.0 三档差异被玻璃霜感吃掉、深色三档像素级相同的修正）
- 可读性分工重构：所有文字（控制行/编辑条/隐藏托盘）坐到卡面材质上，壳层纯玻璃不再直露文字——通透/均衡档浅色为 0 tint 素颜原生玻璃；卡片与控制区之间旧分隔线退役（卡面自有边界）
- 深色外观三档也拉开档差（黑 tint 0.76/0.70/0.82，白字 AAA 物理下限内），卡面亮度上限严格单调，不再"深色三档长得一样"
- 六组参数（三档 × 双外观）全部经可读性证明表重锁（卡面文字 ≥7 AAA、亮面下限、穿透摆幅按档阈值 0.62/0.38/0.10）；「降低透明度」系统开关优先级不变；隐藏托盘修复双重卡纱叠加

## v1.24.1 — 2026-09-09

- 修复通透档控制行与玻璃语言割裂（用户反馈「设置下面那一栏跟上面的液态玻璃格格不入」）：v1.24.0 给控制行垫的整块卡纱板挡不住壁纸亮部，幽灵穿透让底部看着"发脏"，一整块板也与上方"一张张浮卡"的玻璃语言断裂
- 控制行回归玻璃家族：拆掉垫板，每行自成一颗粒 tint 玻璃药丸（clear interactive 玻璃 + 浓 tint，浅白 0.5 / 深黑 0.9）——玻璃从药丸间流过，行像控件一样浮在壳层玻璃上；行距 0→4 留出玻璃缝
- 可读性不降反升：行文字坐药丸 tint，证明锁浅黑字 12.3:1 / 深白字 8.0:1（≥7 AAA），另加药丸亮度语义不变式断言；722→726 测试全绿

## v1.24.2 — 2026-09-09

- 面板材质「通透」档更名**「液态玻璃」**（用户命名）：它本来就是 Apple 原生 Liquid Glass（macOS 26 `glassEffect(.clear)`，壁纸折射/流动/边缘镜面全是系统材质本体），名字应当把它认出来；设置页副标同步为「Apple 原生 Liquid Glass，壁纸折射与流动最真实」
- 档位存储键不变（`clear`）——老用户配置无缝升级，选项顺序与观感零变化；新增命名锁定断言（显示名=液态玻璃、rawValue=clear 防回归）；726→728 测试全绿

## v1.25.0 — 2026-09-09

- 视觉氛围感精细化（用户清单盘点：柔和渐变/流体波浪/低电呼吸/高温边框四项中三项半已在此前版本落地，本轮补上最后一块）：**弧端流光晕**——充电时一小段白色高光沿进度弧从 12 点流向弧端，到站即隐、循环往复，能量沿弧线流入、汇入弧端光点的隐喻（白色就是光点的颜色：流光是还没到达的光点）
- 流速与波速同一物理口径：`arcFlowPeriod` 复用 `wavePeriod` 功率映射再量化 0.3s 档（≤10W→2.4s、≥60W→1.2s，约每 12W 一档）——量化防 SMC 采样抖动让光段沿弧瞬移；光段长度随弧长收缩封顶 20°，永远短于进度弧本身
- 纪律全沿用：12fps 限帧、纯几何位移、无模糊无 repeatForever；「减少动态效果」整层退场（纯动效零信息，不似波浪留静面语义）；可见性走 mood 门（`--miao-visual=charging` 注入可照出）；令牌 静息 0.16 < 波浪 0.26 < 流光 0.30 亮度阶梯锁进证明表
- 修正过程：首版用 accent 色光晕叠加饱和弧，像素仲裁证实不可见且 `.task` 相位累加器在该宿主下不推进——改白色高光 + 全屋统一 TimelineView 墙钟模式后四帧互证流动在案；728→741 测试全绿

## v2.0.0 — 2026-09-12

- **充电场景模式预设（核心功能优化计划 M1-C1 转正）**：设置页新增「充电场景」区——办公 / 外出满充 / 长期存放 / 快充四档，一键切换「保养线 + 低电警示线 + 提醒/免打扰」组合；模式＝配置预设（D1 裁决后无任何充电干预，其余设置原样保留）。回显从 `ScenarioPreset.matched` 反查真实落盘配置：手动改任何阈值即显示「自定义」，切换失败不会被界面谎报成功
- **可发布性修复（构建环境事故）**：CLT 自动升级 27.0.0 后工具链不再携带 SwiftUIMacros 宏插件，而 SDK 27 起 SwiftUI 属性包装器改为宏实现，UI 层编译全面断闸；`build.sh` 在 CLT-only 环境下自动钉住 MacOSX26.5 SDK（26.5 的 SwiftUI 尚是 property wrapper 实现，不依赖宏插件），装完整 Xcode 后自动回落默认 SDK
- **编译盲区两个实锤缺陷（新审计门抓出）**：上轮测试门现代化把 UI 层排除出一切自动编译信号，96d1d29 在盲区里带进两处必挂——①设置页插入场景预设区时把 `thresholdControls` 的 `@ViewBuilder` 属性挤走（Swift 6 opaque 推断失败）②从 GlassStyle 抽出 GlassTokens 时顺手删掉了 `import SwiftUI`。均已修复；CI 新增全量源码 Swift 6 审计步（runner 完整 Xcode 自带宏插件），恢复「CI 绿 = 可构建」等式；测试门排宏清单补 @Environment/@Observable，宏宿主排除改为显式回显清单
- **调试注入口按 2.0 计划退役**：`--miao-visual` 与 `MIAODIAN_DEBUG_MATERIAL` 全部移除（四态/三档目检使命完成）；`MIAODIAN_DEBUG_OPEN_PANEL` 保留（ADR-5：截图流验收唯一程序化开面板通道）
- 测试 772→765（移除 7 条注入解析断言）；Swift 6 全量审计零错误；CI 绿

## v2.0.1 — 2026-09-12

- 文案诚实修补：**长期存放**预设的说明补上「夜间免打扰」——该预设会把夜间免打扰组合（23–8 点）一并打开，此前四档说明里唯独它漏说自己对免打扰的动作（办公写明「夜间免打扰」、外出满充「全天候提醒」、快充「免打扰关闭」）
- 新增文案诚实不变式断言：凡把免打扰打开的预设，说明文字必须写明——防未来新增预设静默改开关不告知；测试 765→767

## v2.0.2 — 2026-09-12

- **说明即承诺（打磨轮·静默失败透镜）三处收口**：①「外出满充」的说明承诺「低电警示提前到 25%」，但低电量提醒开关此前不由预设管辖——用户关着它时承诺静默落空；现在该预设自己打开 `.alertLowBattery`（matches 同步认领，手动关掉即诚实回「自定义」），说明里承诺的开关一律由预设配置；②新增**送达诚实警告**：预设态 +「启用提醒通知」总开关关闭时，设置页以橙色脚注写明「以上提醒不会送达」——承诺了提醒就要交代送达条件（`ScenarioPreset.deliveryWarning` 纯函数）；③自定义态覆盖警告的枚举补准（含相关提醒开关）
- 审计后无恙：保养拔电提醒为电平触发+每次插电去重——插着电停在 95% 时选「办公」，下一帧即收到「已充到 95%」，承诺不落空；免打扰静默后补发机制（v1.19.1）覆盖预设场景
- 测试 767→772（travel 开关认领/偏离脱预设/送达警告三态，均变异检验）

## v2.0.3 — 2026-09-13

- **整个面板都是液态玻璃（用户裁决）**：液态玻璃档下卡面从平涂白块改为真玻璃——`GlassTokens.cardIsGlass` 单一归属判定，卡纱浓度进 `.clear.tint`（与壳层/证明表同源），壁纸在卡面下流动，整个面板一族玻璃；均衡/厚重档磨砂卡面即档位语义保持平涂，降低透明度下不叠玻璃（壳已不透明）
- 卡面材质归属锁进证明表：液态玻璃=真玻璃 / 均衡·厚重=磨砂 / 降低透明度一律不叠，三态变异检验必红
- 预览 harness 取景升级：隐藏红绿灯、面板贴菜单栏位取景（正后方纯壁纸，截图不带用户窗口）；`run_preview.sh` 补 CLT-only SDK 自适应（与 build.sh 同款，CLT 27 缺宏插件时钉 26.5）

## v2.1.0 — 2026-09-15

- **系统充电共存（M1-C2）**：面板新增「系统正在暂缓充电 · 点按打开电池设置」行——插电却不在充时解释原因并给出一键通道。判定为纯读：`AppleSmartBattery → ChargerData → NotChargingReason` 的 0x01000000 位，且必须「插电 + 不在充 + 未充满」；读不到签名不下结论。签名标定跨版本成立（macOS 26.6.2 与 27.0 均实测按住=16777216），充电中负例亦已实测（27.0 恢复充电时 reason=0，8 帧连续），`isCharging` 门保留作跨机型防御
- **保养提醒与系统不再打架**：系统已在保养线（±3%）处按住充电时，本插电会话不再重复喊「可以拔了」；拔电重置。修一处陈旧静默缺陷——重置原先被保养提醒开关的 guard 挡在门外，「关着提醒拔电→再插电→再开提醒」会顶着上一会话的置位吞掉本该发的提醒；改为先于 guard 的纯函数状态转移并补断言
- **洞察文案与机制对账**：「系统在替你保养，二选一」这条洞察此前暗示用户不选就会被反复提醒；静默真的生效过（本机签名可读且覆盖保养线）才改用「暂停期间我们已不再重复提醒你拔电」的措辞，读不到签名的机器保留原措辞——不承诺做不到的事
- **macOS 27 适配（构建环境）**：SDK 选择从「猜环境路径名」改为**能力探测**（`工具/sdk选择.sh`，build.sh 与预览 harness 共用）——拿 `@State` 探针实测当前工具链能编哪个 SDK，能编用默认，否则按版本从新到旧回落，全失败则停下给可执行指引；旧判据在「干净 CLT 27（无 26.5 遗留）」的新用户机器上会让构建崩在半路。测试面回显实际使用的 SDK 版本，逻辑层信号（本机 SDK 27）与 UI 层钉的旧 SDK 分开读
- **SDK 27 弃用清零**：`String(cString:)` 改为按 byteCount 与首个 NUL 双截断的显式解码（坏字节走替换字符，不因局部损坏丢掉整条进程路径），Swift 6 全量审计恢复零错误零警告
- **可读性审查（对抗式，三法互证：解析模型 / 项目证明函数 / 真实像素）**：控制行与警示行的文字此前直接坐 `accentColor`/橙色，实测最坏 1.2~2.4:1（同处 `.primary` 为 12.3/8.0:1）——两行已改走规范组件 `PopoverActionRow` 的自适应 label 色，并把「彩色小字坐玻璃面不达 AA」锁进证明表（6 条负向锁，未来改 tint 会先变红逼一次显式决策）。**已知例外**：面板仍有约 6 处语义色数值（当前功率、24h 读数、高峰时点、充电记录终值、编辑条「完成」、健康趋势区间）未纳入证明，实测 1.1~2.4:1，整改方向与色板明度阈值（浅色档需 L≤0.049、深色档需 L≥0.616 才能达 AA）已入队列
- 门面：README 可读性声明按证明实际覆盖面收窄（中英文同口径），提醒种类 14→15、测试数 821；`project.pbxproj` 部署目标 26.0→15.0 与产物一致
- 测试 778 → 821 项全绿（新增：C2 判定 13、采样边沿 4、共存状态转移 4、洞察措辞 6、控制行文字色锁 6、路径解码 10）；六组反向实现变异检验全部抓红

## v2.2.0 — 2026-09-18（玻璃优先观感 + 面板滚动 + 苹果手感 + 全面巡查修复）

两件事同属一条线：v2.1.0 审查挂账的可读性整改，与「面板装不下 / 滚动手感 / 交付前对抗巡查」。

**玻璃优先（文字全面走已证明的自适应色）**
- 彩色小字坐透玻璃不达 AA → primary 化 + `labelOnGlass`；语义进文案；证明表负向锁

**面板高度与滚动**
- `PanelFit` 装不下才滚；SwiftUI ScrollView 卡片区；壳/头/控固定
- 触控板 120Hz：`CardFrameStore` 滚动静默写表；`defersUISnapshot` 滚动中不发 @Published；动效门；打开面板 `scrollActivity.reset()`
- **禁止**玻璃 `drawingGroup`（离屏发黑）；橡皮筋/滚动条/头部 TimelineView 30fps 保留液态玻璃生命感

**全面巡查修复（交付前自检）**
- **CRITICAL**：配置解码失败不得 `save(.default)` 覆盖用户字节；`PanelLayout` 全字段 `decodeIfPresent`
- 滚动中电量回滚丢弃过时 pending；拖拽出界不提交旧 preview；`resetLayout` 走后悔药；关面板复位 `publishesChanges`
- `rowSource` 弹簧过 `PanelMotionGate`；宿主 `didTune` 可按签名补挂观察器；头部动画接 `reduceMotion`
- 门面数字按实测对齐；版本 **2.2.0**

**门**：测试 **943** 全绿；Swift 6 全量审计零错误；离屏高度 973≤987；交付前自检六步全过


两件事同属一条线：v2.1.0 的审查挂账"少数语义色数值不达 AA"，整改时又发现撤色暴露了面板本来就没有排版层次。v2.1.1 从未打 tag 发布，故与观感整改合并为一条，不伪造发布史。

**玻璃优先（文字全面走已证明的自适应色）**

- 数学事实：彩色小字坐透玻璃面最坏 1.06~2.46:1，而 systemBlue 即使坐在 100% 不透明白底上也只有 4.02:1——**透玻璃与彩色小字不能共存**，要保彩色就得让底接近不透明（蓝需 α≥0.99），那就不再是玻璃。用户裁决：保玻璃
- `BatteryInfoItem.valueTint` 通道整体删除，异常语义迁进文案：「（偏低）」「（偏高）」「（偏快）」，「疑似慢充」本就在字里；图标色专职表身份，不再兼任第二套告警色
- primary 化共 26 处（含穷举扫描补上的 16 处——第一轮 grep 只抓字面量，漏了三元与变量式写法）；`Color.secondary` 5 处与 `Color.tertiary` 11 处一并改 `labelOnGlass`（token 注释早写着 secondary 透底最坏 2.7:1 不达 AA）；徽章文字走 label 色、语义色留在胶囊底（图形）；调试徽章白字坐实橙底 2.2:1 改深字
- 证明表扩 18 条「卡面文字色锁」（六组材质×双外观×蓝/橙/绿），与 v2.1.0 的 6 条药丸锁共用同一色表：谁把彩色文字放回玻璃面，改 tint 或改色都会先撞锁
- 清掉一条 9 天未清的临时诊断：每次开面板往 error 级日志写两行 `frame=…`；根因（面板随 iBar 收纳漂移）早已用屏幕锚定修好，而标题写着「清理:临时帧诊断日志」的 adb540b 实际是把这两行**加了进来**。删除后 error 日志实测归零
- README 可读性声明恢复完整口径（"面板上每一处文字"）

**观感整改（用户反馈"丑丑的" → 实拍诊断）**

- 根因不是新样式丑，是撤色把"面板没有排版层次"这件事暴露了：标签与值同字号、右对齐长短值把值列左缘撕成锯齿、把手白胶囊压在首行、重复事件占满卡高、状态行与动作行同形
- 信息行两列化 + 字阶拉开（标签 11.5 regular／值 12.5 semibold，值左对齐固定列起点）；值改两行上限，并新增「截断守卫」测试——撤色时把语义追加到值末尾，而长值本来就会被截断，等于"说了但看不见"；守卫第一次跑就抓到充电器与睡眠掉电两处，改为**判断前置**（结论紧跟名字，被截的只能是尾部明细）
- 电源事件显示折叠：相邻同类且 ≤30 分钟折成「接上电源 ×6 · 11:40–12:17」；跨类型绝不合并（插拔插折两条会抹掉事件顺序），超间隔不合并（相隔几小时的两插不能看着像一次长插），逐条时刻留在悬停说明
- 拖拽把手常态只留淡符号（胶囊底改 hover/拖动时出现，位置与可抓取性不变）；控制行浅档 tint 0.5→0.62 压住高对比壁纸的明暗条带（黑字 12.3→14.4:1），状态行与动作行分节、动作分三组
- **可用性修复（比"丑"要紧）**：像素量出可视区仅 987pt（菜单栏 32 + Dock 98），而 12 张卡全开时面板 1076pt——底部"导出数据/电池设置/退出"整段压在 Dock 下点不到。逐层收高度：卡内边距 7→3、信息行 3→1.5、药丸间距 4→3、卡间距 4→3、事件卡 6→4 行、充电记录 3→2 条；并去掉两处把值挤成两行的冗余（额定等于协商值时不再重复、可选档位单位只写一次 `15/27/36/45/✓70W`）
- **最终测量（离屏量具与实拍互证）**：用户卡片集下面板 1101pt（离屏 fittingSize 连测 4 次稳定）/ 实拍底部压进 Dock 89pt。间距压缩已到极限，救不了它——根治要么让卡片区可滚动（结构改动，必须实拍验收），要么由用户隐藏 1–2 张卡。已作为决策点交用户，不在没有视觉通道时动结构
- 头部右侧空块兜底：体检评分被关掉时该槽改放"接的是哪只充电器"（名字 + 协议·额定）——这个位置本就是为填该空块而设，关掉体检后空块回来了
- 面板右缘锚定改用 `visibleFrame`（Dock 停右侧时整屏右缘会被 constrainFrameRect 挤窄）
- 更正两条假信号：CGWindowList 报的面板宽 526 不可信（像素实测 586pt 正常）；窗口尺寸只信像素测量
- **装不下才滚动（高度问题的结构收口）**：`PanelFit` 纯函数按「可视区 − 顶部余量 − 隐形标题栏」算预算，自然高超出才启用**卡片区内部滚动**——外壳/头部/控制行固定，玻璃不跟滚重采样；装得下的用户走原路径零行为变化。实测用户卡片集 1101pt vs 可用 951pt，滚动启用；info 级日志记录，不污染 error 健康检查。**滚动模式下关闭整卡拖拽**（`allowsCardDrag` + gesture mask）：simultaneousGesture 与 ScrollView 抢同一段 pan，想滚却会把卡提起来；把手（0 距离）仍是明示抓取点
- **滚动掉帧修复**：frame 探针/拖拽手势改走板面内容坐标系（`BoardSpace`），不再用 `.global`——滚动时 global frame 每帧变、探针写回状态表、整面板重算，是掉帧主因；坐标系必须挂在**滚动内容**上（挂祖先 ScrollView 外仍会每帧变）
- **鼠标滚轮跟手**：触控板精确滚动已跟手，鼠标是行步进（一格约 ±1），在玻璃卡片区里「转很多圈才动一点」。`ScrollFeel` 纯函数判定「非精确 + 幅度有效」→ `AppScrollFeel` 本地监视合成 `units: .pixel` 的滚轮事件（默认 60pt/格）再吞掉原事件——面板与设置 Form 共用；合成事件本身是精确滚动，不会自我递归
- **动效丝滑（液态玻璃有生命且不掉帧）**：头部呼吸/波浪/流光/热脉冲 **12→30fps**（小面积几何，跟手且便宜）；数值淡化 0.3→0.2s；重排弹簧阻尼略增少回弹；**动效门**——滚动中不跑装饰性重排/折叠弹簧、不挂把手层与 frame 探针（拖拽跟手动画始终保留）；橡皮筋/滚动条/玻璃材质原样
- **对抗审查修复（同轮）**：探针测量失败踩 `floorHeight` 地板（不做 0 高空白面板）；鼠标滚轮合成事件在 `event.window == nil` 时退回系统路径（防滚到别的窗口）；滚动活动 Task 可取消（不再 120Hz 叠任务）
- **拖拽人体工学**：抓住卡片任意处即可拖（10pt 阈值避开误触，把手保留 0 距离作明示抓取点；仅非滚动宿主）；落点指示线画在将插入的缝隙上（几何唯一来源 `CardDropResolver.indicatorLine`，排除被拖卡；出界时清线不冻在上一缝）；让位弹簧 response 0.32→0.24、damping 0.82→0.92，减少过冲
- 刻意未做（等眼睛）：12 张卡折叠摘要的 6 种字号/字重混搭统一、空态卡进一步收高；**滚动宿主的实拍终验**（离屏只验尺寸不验观感，用户在场时未抢屏）

**门**：测试 **924** 项全绿（含 ScrollFeel/PanelScrollIdle/Elasticity/PanelScrollGeometry/测量失败地板）；Swift 6 全量审计零错误零警告；变异：弹性开关、offset 钳制写反均被抓红；universal 出包并装机
