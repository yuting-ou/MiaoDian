# 妙电 · 液态玻璃改造 需求文档

> 版本：v1.2（2026-09-05 定稿）　|　配套：《架构文档.md》《玻璃改造报告.md》《液态玻璃计划.md》
> 本文档是需求的唯一权威来源。需求变更必须落字到 §七 演进记录，不许口头改。

## 一、背景与目标

妙电（MiaoDian）是 macOS 菜单栏电池管理应用，核心价值是「一眼看清很多真的数据」。macOS 26
带来 Liquid Glass 设计语言，本项目把状态面板与设置窗口迁移到该语言，使应用获得系统原生质感。

一句话使命：**面板要有苹果液态玻璃的质感，但一个数字都不能因此变难读。**

## 二、需求优先级（冲突时的裁决顺序，不可谈判）

1. **可读性**——妙电的价值是数据，玻璃是容器不是主角。
2. **质感**——通透、折射、交互弹性，服务于"高级、克制、像系统原生"。
3. 版本数、改动量——不是 KPI。

裁决规则一句话：**玻璃让一步，可读性不让步。拿不准就退回"少一点玻璃、多一点清晰"。**

## 三、功能需求

| 编号 | 需求 | 说明 |
|---|---|---|
| F1 | 面板液态玻璃化 | 状态面板整体迁移 Liquid Glass 语言（外壳/分区/控件/徽章/仪表分层，见架构文档材质系统） |
| F2 | 范围=面板+设置窗口 | 充电曲线窗口随后（同套 token） |
| F3 | 双路径兼容 | macOS 15.0 部署目标不变；26 走玻璃，15–25 保留原质感降级，降级不得破相 |
| F4 | 辅助功能三开关优雅降级 | 降低透明度/增加对比度/减少动态，各需截图验收 |
| F5 | 无障碍不回退 | 既有 VoiceOver 标签（图表汇总朗读、头部组合朗读）原样保留 |
| F6 | 菜单栏标签不动 | 8 种显示模式（图标/百分比/温度/功率/剩余时间/迷你曲线/轮换/流光）行为不变 |
| F7 | 充电器识别 v2（物理头归并） | 同一只充电头因多口分功率/协议降档协商出不同瓦数时，仍认成同一个头：统计连线、速度对比共享样本；当次功率照实显示并进观察记录 |

## 四、非功能需求（可证伪的验收标准）

| 编号 | 标准 | 判据 |
|---|---|---|
| N1 | 内容不玻璃化 | 文本/曲线/热力图/数字正下方不得有降低对比度的模糊或折射层 |
| N2 | 小字号保护 | 9–12pt 文字所在区域对比度达标，禁止坐在半透明模糊底上 |
| N3 | 对比度硬指标 | 正文 ≥ AA（4.5:1），关键数字目标 AAA（7:1），工具实测不靠肉眼 |
| N4 | 三壁纸实测 | 浅色/深色/花哨彩色壁纸各截一张，逐数字核对 |
| N5 | 语义色不被稀释 | 充电绿/低电红/高温橙辨识度不变；不达标须记录基线对照证明非回归 |
| N6 | 信息密度不倒退 | 卡片数量、每卡行数不得少于改造前（前后截图对比） |
| N7 | 能量红线 | 面板关闭稳态 CPU ≈ 0.1% 基线内；玻璃只在打开时渲染 |
| N8 | 逻辑层零改动 | 480 项测试始终全绿且零警告；变红即回滚 |
| N9 | 兼容编译 | Swift 6 审计（15.0 目标）零错误；CI 绿 |

## 五、明确不做

- 不做"玻璃汤"：不给图表/数字各自套玻璃。
- 不引入第三方玻璃库、不用私有 API、不新增联网、不申请新权限。
- 不在 26 以下强上玻璃导致降级破相。
- 不为美观牺牲数据密度。

## 六、验收流程

每阶段：目标声明 → 实现 → 测试门（480 全绿零警告）→ Swift 6 审计 → 截图验收（工具实测）→
提交 → 报告沉淀。视觉验证纪律：每阶段必须截图核对，无法验证的如实标注"代码就绪待验收"。

## 七、需求演进记录（与 AI 反复对齐的过程，为什么要写成现在这样）

| 轮次 | 需求理解 | 触发 | 修正 |
|---|---|---|---|
| R1 | "上液态玻璃"=卡片全部 glassEffect | 初版《液态玻璃计划》 | 实做后用户反馈面板"发闷"（Menu​BarExtra 宿主灰底不可清） |
| R2 | 透明=清空宿主窗口底 | 用户截图"这是什么"（背后窗口文字从缝隙穿出） | 补面板级玻璃底；发现玻璃采样的是桌面壁纸而非中间窗口 |
| R3 | 通透=clear 玻璃 | 用户"液态玻璃我说了多少遍了"（regular 奶白洗掉壁纸） | 全线 clear，通透达成——但实测深色壁纸黑字 2.5:1 不达标 |
| R4 | 通透必须让位于可读性 | 目标模式作业书（可读性优先版）+ 对比度实测 | 材质映射表回归 Glass.regular；仍不达计算排除后 → ultraThin+自适应亮度地板（最终方案） |
| R5 | 文档化与阶段化 | 用户"落实需求文档、架构文档，拆阶段完成" | 本文档与《架构文档》成文；后续需求变更走 §七 记录 |

**需求本质（R5 定稿）**：用户要的"液态玻璃"= 苹果系统原生的高级质感与克制，
而非最大化透明度。透明度是手段，可读性是约束，两者冲突时约束赢。
